# 资产管理系统 API 接口详细文档

> **版本**: v1.1.0
> **最后更新**: 2026-06-08
> **技术栈**: Django REST Framework + SimpleJWT + drf-spectacular
> **维护团队**: 资产管理系统后端开发组

---

## 目录

- [一、文档基本信息](#一文档基本信息)
- [二、认证与授权](#二认证与授权)
- [三、统一响应格式](#三统一响应格式)
- [四、资源端点列表](#四资源端点列表)
- [五、请求参数与请求体](#五请求参数与请求体)
- [六、响应示例](#六响应示例)
- [七、数据模型](#七数据模型)
- [八、特殊功能说明](#八特殊功能说明)
- [九、错误码完整清单](#九错误码完整清单)
- [十、实用补充内容](#十实用补充内容)

---

## 一、文档基本信息

### 1.1 项目概述

| 项目 | 说明 |
|------|------|
| 项目名称 | 资产管理系统（Asset Management System） |
| 版本号 | v1.1.0 |
| 基础 URL | `http://localhost:8000/api/` |
| 协议 | HTTP/HTTPS RESTful API |
| 数据格式 | JSON |
| 字符编码 | UTF-8 |

### 1.2 在线文档

| 文档类型 | 访问地址 | 说明 |
|----------|----------|------|
| Swagger UI | `/api/swagger/` | 交互式 API 文档，支持在线调试 |
| ReDoc | `/api/redoc/` | 阅读友好的 API 文档 |
| OpenAPI Schema | `/api/schema/` | 原始 OpenAPI 3.0 Schema（JSON/YAML） |

### 1.3 路由前缀

| 模块 | 路由前缀 | 说明 |
|------|----------|------|
| 认证管理 | `/api/auth/` | 用户注册、登录、Token 管理 |
| 用户管理 | `/api/users/` | 部门管理、员工管理 |
| 资产管理 | `/api/assets/` | 资产全生命周期管理 |
| 未登记资产管理 | `/api/unregisteredassets/` | 未登记资产处理与审批 |
| 仪表盘 | `/api/dashboard/` | 数据统计与概览 |

### 1.4 变更日志

| 日期 | 版本 | 变更内容 |
|------|------|----------|
| 2026-06-08 | v1.1.0 | 新增未登记资产管理模块（6个端点）；新增18个端点（认证、部门、员工、合同、资产、出库、回收、已报废、硬盘序列号、操作记录）；删除 search_by_name 端点；修改5个创建端点的字段（资产、出库、回收、待报废、硬盘序列号搜索）；新增枚举值（lowvalue、recycled_pending、damaged、scrapped）；新增路由前缀 /api/unregisteredassets/；更新自动编码格式（资产编码、回收记录编码） |
| 2026-05-23 | v1.0.0 | 初始版本，包含全部 API 端点文档 |

---

## 二、认证与授权

### 2.1 认证方式

系统采用 **JWT (JSON Web Token)** 认证机制，通过 [SimpleJWT](https://django-rest-framework-simplejwt.readthedocs.io/) 实现。

**双 Token 机制：**

| Token 类型 | 有效期 | 用途 |
|------------|--------|------|
| `access_token` | 短期（默认 2 小时） | 访问 API 资源，放在请求头中 |
| `refresh_token` | 长期（默认 7 天） | 用于刷新 access_token |

### 2.2 获取 Token

通过登录接口获取 Token：

```
POST /api/auth/login/
```

成功响应中包含 `access` 和 `refresh` 两个 Token。

### 2.3 请求头格式

所有需要认证的接口，必须在请求头中携带 `access_token`：

```
Authorization: Bearer <access_token>
```

**示例：**

```http
GET /api/auth/profile/ HTTP/1.1
Host: localhost:8000
Authorization: Bearer eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9...
Content-Type: application/json
```

### 2.4 Token 刷新

当 `access_token` 过期后，使用 `refresh_token` 获取新的 `access_token`：

```
POST /api/auth/token/refresh/
```

```json
{
  "refresh": "<refresh_token>"
}
```

### 2.5 权限级别

| 权限级别 | 说明 | 适用范围 |
|----------|------|----------|
| `AllowAny` | 允许所有用户访问（含未认证） | 注册、登录、Token 获取/刷新/验证 |
| `IsAuthenticated` | 需要有效 Token | 大部分业务接口 |
| `IsAdminUser` | 需要管理员权限 | 部门/员工的创建、修改、删除操作 |

### 2.6 认证错误示例

**未认证（401）：**

```json
{
  "code": 401,
  "msg": "未提供认证凭据",
  "data": {}
}
```

**Token 过期（401）：**

```json
{
  "code": 401,
  "msg": "给定令牌对任何令牌类型都无效",
  "data": {}
}
```

**权限不足（403）：**

```json
{
  "code": 403,
  "msg": "您没有权限执行此操作",
  "data": {}
}
```

---

## 三、统一响应格式

### 3.1 成功响应

所有接口统一返回以下格式：

```json
{
  "code": 200,
  "msg": "操作成功",
  "data": {}
}
```

| 字段 | 类型 | 说明 |
|------|------|------|
| `code` | int | HTTP 状态码 |
| `msg` | string | 操作结果描述 |
| `data` | object/array/null | 响应数据 |

### 3.2 分页响应

当接口支持分页且请求中包含 `page` 或 `page_size` 参数时，返回分页格式：

```json
{
  "code": 200,
  "msg": "查询成功",
  "data": {
    "count": 100,
    "total_pages": 5,
    "page": 1,
    "page_size": 20,
    "next": "http://localhost:8000/api/assets/assets/?page=2&page_size=20",
    "previous": null,
    "results": [...]
  }
}
```

| 字段 | 类型 | 说明 |
|------|------|------|
| `count` | int | 总记录数 |
| `total_pages` | int | 总页数 |
| `page` | int | 当前页码 |
| `page_size` | int | 每页数量 |
| `next` | string/null | 下一页 URL |
| `previous` | string/null | 上一页 URL |
| `results` | array | 当前页数据列表 |

### 3.3 错误响应

```json
{
  "code": 400,
  "msg": "操作失败",
  "data": {
    "errors": {
      "field_name": ["错误信息1", "错误信息2"]
    }
  }
}
```

### 3.4 业务错误码表

| 错误码 | 异常类 | HTTP 状态码 | 说明 |
|--------|--------|-------------|------|
| E001 | AppValidationError | 400 | 数据验证失败 |
| E002 | NotFoundError | 404 | 资源不存在 |
| E003 | PermissionDeniedError | 403 | 权限不足 |
| E004 | BusinessLogicError | 400 | 业务逻辑错误 |
| E005 | ResourceConflictError | 409 | 资源冲突 |
| E006 | InternalError | 500 | 服务器内部错误 |

---

## 四、资源端点列表

### 4.1 认证管理模块 (`/api/auth/`)

| 方法 | URL | 描述 | 权限 |
|------|-----|------|------|
| POST | `/api/auth/register/` | 用户注册 | AllowAny |
| POST | `/api/auth/login/` | 用户登录 | AllowAny |
| POST | `/api/auth/logout/` | 退出登录 | IsAuthenticated |
| GET | `/api/auth/profile/` | 获取当前用户信息 | IsAuthenticated |
| PUT | `/api/auth/profile/` | 更新当前用户信息 | IsAuthenticated |
| POST | `/api/auth/token/` | 获取 Token（SimpleJWT 标准） | AllowAny |
| POST | `/api/auth/token/refresh/` | 刷新 Token | AllowAny |
| POST | `/api/auth/token/verify/` | 验证 Token | AllowAny |
| GET | `/api/auth/users/list_active/` | 获取所有激活用户 | IsAdminUser |

### 4.2 部门管理 (`/api/users/departments/`)

| 方法 | URL | 描述 | 权限 |
|------|-----|------|------|
| GET | `/api/users/departments/` | 部门列表 | IsAuthenticated |
| POST | `/api/users/departments/` | 创建部门 | IsAdminUser |
| GET | `/api/users/departments/{department_code}/` | 部门详情 | IsAuthenticated |
| PUT | `/api/users/departments/{department_code}/` | 全量更新部门 | IsAdminUser |
| PATCH | `/api/users/departments/{department_code}/` | 部分更新部门 | IsAdminUser |
| DELETE | `/api/users/departments/{department_code}/` | 删除部门（软删除） | IsAdminUser |
| GET | `/api/users/departments/{department_code}/employees/` | 获取部门下的员工 | IsAuthenticated |
| GET | `/api/users/departments/tree/` | 获取部门树形结构 | IsAuthenticated |
| GET | `/api/users/departments/{department_code}/children/` | 获取子部门 | IsAuthenticated |
| GET | `/api/users/departments/{department_code}/path/` | 获取部门路径（面包屑导航） | IsAuthenticated |
| GET | `/api/users/departments/{department_code}/descendants/` | 获取所有后代部门 | IsAuthenticated |
| PUT | `/api/users/departments/{department_code}/move/` | 移动部门 | IsAdminUser |
| PUT | `/api/users/departments/sort/` | 批量排序部门 | IsAdminUser |

### 4.3 员工管理 (`/api/users/employees/`)

| 方法 | URL | 描述 | 权限 |
|------|-----|------|------|
| GET | `/api/users/employees/` | 员工列表 | IsAuthenticated |
| POST | `/api/users/employees/` | 创建员工 | IsAdminUser |
| GET | `/api/users/employees/{employee_jobcode}/` | 员工详情 | IsAuthenticated |
| PUT | `/api/users/employees/{employee_jobcode}/` | 全量更新员工 | IsAdminUser |
| PATCH | `/api/users/employees/{employee_jobcode}/` | 部分更新员工 | IsAdminUser |
| DELETE | `/api/users/employees/{employee_jobcode}/` | 删除员工（软删除） | IsAdminUser |
| GET | `/api/users/employees/search/` | 全局搜索员工 | IsAuthenticated |
| GET | `/api/users/employees/statistics/` | 员工统计 | IsAuthenticated |
| GET | `/api/users/employees/active_employees/` | 在职员工列表 | IsAuthenticated |
| POST | `/api/users/employees/{employee_jobcode}/change_status/` | 更改员工状态 | IsAdminUser |
| PUT | `/api/users/employees/sort/` | 批量更新员工排序 | IsAdminUser |

### 4.4 仓库管理 (`/api/assets/storages/`)

| 方法 | URL | 描述 | 权限 |
|------|-----|------|------|
| GET | `/api/assets/storages/` | 仓库列表 | IsAuthenticated |
| POST | `/api/assets/storages/` | 创建仓库 | IsAuthenticated |
| GET | `/api/assets/storages/{storage_code}/` | 仓库详情 | IsAuthenticated |
| PUT | `/api/assets/storages/{storage_code}/` | 全量更新仓库 | IsAuthenticated |
| PATCH | `/api/assets/storages/{storage_code}/` | 部分更新仓库 | IsAuthenticated |
| DELETE | `/api/assets/storages/{storage_code}/` | 删除仓库（软删除） | IsAuthenticated |
| GET | `/api/assets/storages/statistics/` | 仓库统计 | IsAuthenticated |

### 4.5 资产类型管理 (`/api/assets/asset-types/`)

| 方法 | URL | 描述 | 权限 |
|------|-----|------|------|
| GET | `/api/assets/asset-types/` | 资产类型列表 | IsAuthenticated |
| POST | `/api/assets/asset-types/` | 创建资产类型 | IsAuthenticated |
| GET | `/api/assets/asset-types/{asset_type_code}/` | 资产类型详情 | IsAuthenticated |
| PUT | `/api/assets/asset-types/{asset_type_code}/` | 全量更新 | IsAuthenticated |
| PATCH | `/api/assets/asset-types/{asset_type_code}/` | 部分更新 | IsAuthenticated |
| DELETE | `/api/assets/asset-types/{asset_type_code}/` | 删除 | IsAuthenticated |

### 4.6 合同管理 (`/api/assets/contracts/`)

| 方法 | URL | 描述 | 权限 |
|------|-----|------|------|
| GET | `/api/assets/contracts/` | 合同列表 | IsAuthenticated |
| POST | `/api/assets/contracts/` | 创建合同 | IsAuthenticated |
| GET | `/api/assets/contracts/{contract_code}/` | 合同详情 | IsAuthenticated |
| PUT | `/api/assets/contracts/{contract_code}/` | 全量更新 | IsAuthenticated |
| PATCH | `/api/assets/contracts/{contract_code}/` | 部分更新 | IsAuthenticated |
| DELETE | `/api/assets/contracts/{contract_code}/` | 删除（软删除） | IsAuthenticated |
| GET | `/api/assets/contracts/search/` | 全局搜索合同 | IsAuthenticated |
| GET | `/api/assets/contracts/getcontractByname/{name}/` | 按名称搜索合同 | IsAuthenticated |
| GET | `/api/assets/contracts/statistics/` | 合同统计 | IsAuthenticated |
| POST | `/api/assets/contracts/{contract_code}/payment_record/` | 添加付款记录 | IsAuthenticated |
| POST | `/api/assets/contracts/{contract_code}/update_settlement_status/` | 更新合同结算状态 | IsAuthenticated |

### 4.7 资产管理 (`/api/assets/assets/`)

| 方法 | URL | 描述 | 权限 |
|------|-----|------|------|
| GET | `/api/assets/assets/` | 资产列表 | IsAuthenticated |
| POST | `/api/assets/assets/` | 创建资产（支持批量） | IsAuthenticated |
| GET | `/api/assets/assets/{asset_code}/` | 资产详情 | IsAuthenticated |
| PUT | `/api/assets/assets/{asset_code}/` | 全量更新 | IsAuthenticated |
| PATCH | `/api/assets/assets/{asset_code}/` | 部分更新 | IsAuthenticated |
| DELETE | `/api/assets/assets/{asset_code}/` | 删除（软删除） | IsAuthenticated |
| GET | `/api/assets/assets/getassetbyname/{name}/` | 按名称搜索资产 | IsAuthenticated |
| GET | `/api/assets/assets/search/` | 搜索资产 | IsAuthenticated |
| GET | `/api/assets/assets/statistics/` | 资产统计 | IsAuthenticated |
| GET | `/api/assets/assets/search_available/` | 获取可用资产 | IsAuthenticated |
| POST | `/api/assets/assets/{asset_code}/change_status/` | 变更资产状态 | IsAuthenticated |
| GET | `/api/assets/assets/combined_details/` | 资产+合同组合详情 | IsAuthenticated |
| GET | `/api/assets/assets/getassetbyrecordcode/{asset_recordcode}/` | 按入库记录编码搜索资产 | IsAuthenticated |
| GET | `/api/assets/assets/contract_by_asset/{asset_code}/` | 通过资产编码查询关联合同 | IsAuthenticated |

### 4.8 出库管理 (`/api/assets/out-assets/`)

| 方法 | URL | 描述 | 权限 |
|------|-----|------|------|
| GET | `/api/assets/out-assets/` | 出库列表 | IsAuthenticated |
| POST | `/api/assets/out-assets/` | 创建出库记录 | IsAuthenticated |
| GET | `/api/assets/out-assets/{outasset_recordcode}/` | 出库详情 | IsAuthenticated |
| PUT | `/api/assets/out-assets/{outasset_recordcode}/` | 全量更新 | IsAuthenticated |
| PATCH | `/api/assets/out-assets/{outasset_recordcode}/` | 部分更新 | IsAuthenticated |
| DELETE | `/api/assets/out-assets/{outasset_recordcode}/` | 删除 | IsAuthenticated |
| GET | `/api/assets/out-assets/recyclable/` | 可回收资产列表 | IsAuthenticated |
| GET | `/api/assets/out-assets/by-asset/{asset_code}/` | 按资产查出库记录 | IsAuthenticated |
| GET | `/api/assets/out-assets/by-applicant/{applicant_jobcode}/` | 按申请人工号查出库记录 | IsAuthenticated |
| GET | `/api/assets/out-assets/statistics/` | 出库统计 | IsAuthenticated |

### 4.9 回收管理 (`/api/assets/recycle-assets/`)

| 方法 | URL | 描述 | 权限 |
|------|-----|------|------|
| GET | `/api/assets/recycle-assets/` | 回收列表 | IsAuthenticated |
| POST | `/api/assets/recycle-assets/` | 创建回收记录 | IsAuthenticated |
| GET | `/api/assets/recycle-assets/{outasset_recordcode}/` | 回收详情 | IsAuthenticated |
| PUT | `/api/assets/recycle-assets/{outasset_recordcode}/` | 全量更新 | IsAuthenticated |
| PATCH | `/api/assets/recycle-assets/{outasset_recordcode}/` | 部分更新 | IsAuthenticated |
| DELETE | `/api/assets/recycle-assets/{outasset_recordcode}/` | 删除 | IsAuthenticated |
| GET | `/api/assets/recycle-assets/by-asset/{asset_code}/` | 按资产查回收记录 | IsAuthenticated |
| GET | `/api/assets/recycle-assets/by-outasset/{outasset_recordcode}/` | 通过出库记录编码查询回收记录 | IsAuthenticated |

### 4.10 待报废管理 (`/api/assets/damaged-assets/`)

| 方法 | URL | 描述 | 权限 |
|------|-----|------|------|
| GET | `/api/assets/damaged-assets/` | 待报废列表 | IsAuthenticated |
| POST | `/api/assets/damaged-assets/` | 创建待报废申请 | IsAuthenticated |
| GET | `/api/assets/damaged-assets/{damaged_asset_code}/` | 待报废详情 | IsAuthenticated |
| PUT | `/api/assets/damaged-assets/{damaged_asset_code}/` | 全量更新 | IsAuthenticated |
| PATCH | `/api/assets/damaged-assets/{damaged_asset_code}/` | 部分更新 | IsAuthenticated |
| DELETE | `/api/assets/damaged-assets/{damaged_asset_code}/` | 取消待报废申请 | IsAuthenticated |
| POST | `/api/assets/damaged-assets/{damaged_asset_code}/approve/` | 审批通过 | IsAuthenticated |
| POST | `/api/assets/damaged-assets/{damaged_asset_code}/reject/` | 审批拒绝 | IsAuthenticated |
| GET | `/api/assets/damaged-assets/by-asset/{asset_code}/` | 按资产查待报废记录 | IsAuthenticated |

### 4.11 已报废管理 (`/api/assets/waste-assets/`)

| 方法 | URL | 描述 | 权限 |
|------|-----|------|------|
| GET | `/api/assets/waste-assets/` | 已报废列表 | IsAuthenticated |
| GET | `/api/assets/waste-assets/{asset_code}/` | 已报废详情 | IsAuthenticated |
| GET | `/api/assets/waste-assets/by-asset/{asset_code}/` | 按资产查已报废记录 | IsAuthenticated |
| GET | `/api/assets/waste-assets/statistics/` | 已报废统计 | IsAuthenticated |
| GET | `/api/assets/waste-assets/by-date-range/` | 按日期范围查询已报废记录 | IsAuthenticated |

### 4.12 硬盘序列号管理 (`/api/assets/harddisk-sn/`)

| 方法 | URL | 描述 | 权限 |
|------|-----|------|------|
| GET | `/api/assets/harddisk-sn/` | 硬盘序列号列表 | IsAuthenticated |
| POST | `/api/assets/harddisk-sn/` | 创建硬盘序列号 | IsAuthenticated |
| GET | `/api/assets/harddisk-sn/{asset_code}/` | 按资产查硬盘序列号 | IsAuthenticated |
| POST | `/api/assets/harddisk-sn/search_by_serial_number/` | 按序列号搜索 | IsAuthenticated |
| POST | `/api/assets/harddisk-sn/batch/` | 批量保存硬盘序列号记录 | IsAuthenticated |
| GET | `/api/assets/harddisk-sn/by-asset/{asset_code}/` | 按资产查硬盘序列号 | IsAuthenticated |

### 4.13 操作记录 (`/api/assets/operation-logs/`)

| 方法 | URL | 描述 | 权限 |
|------|-----|------|------|
| GET | `/api/assets/operation-logs/` | 操作记录列表（只读） | IsAuthenticated |
| GET | `/api/assets/operation-logs/{pk}/` | 操作记录详情 | IsAuthenticated |
| GET | `/api/assets/operation-logs/recent/` | 最近操作记录 | IsAuthenticated |
| GET | `/api/assets/operation-logs/user/{operator_jobcode}/` | 用户操作记录 | IsAuthenticated |
| GET | `/api/assets/operation-logs/by-logging-id/{logging_id}/` | 通过LoggingId查询操作记录 | IsAuthenticated |
| GET | `/api/assets/assets/{asset_code}/history/` | 资产操作历史 | IsAuthenticated |
| GET | `/api/assets/assets/{asset_code}/timeline/` | 资产状态变更时间线 | IsAuthenticated |

### 4.14 未登记资产管理 (`/api/unregisteredassets/`)

| 方法 | URL | 描述 | 权限 |
|------|-----|------|------|
| GET | `/api/unregisteredassets/unregistered-assets/` | 未登记资产列表 | IsAuthenticated |
| POST | `/api/unregisteredassets/unregistered-assets/` | 创建未登记资产 | IsAuthenticated |
| GET | `/api/unregisteredassets/unregistered-assets/{unregistered_code}/` | 未登记资产详情 | IsAuthenticated |
| PUT | `/api/unregisteredassets/unregistered-assets/{unregistered_code}/` | 全量更新 | IsAuthenticated |
| PATCH | `/api/unregisteredassets/unregistered-assets/{unregistered_code}/` | 部分更新 | IsAuthenticated |
| DELETE | `/api/unregisteredassets/unregistered-assets/{unregistered_code}/` | 删除 | IsAuthenticated |
| POST | `/api/unregisteredassets/unregistered-assets/{unregistered_code}/approve/` | 审批处理 | IsAuthenticated |

### 4.15 仪表盘 (`/api/dashboard/`)

| 方法 | URL | 描述 | 权限 |
|------|-----|------|------|
| GET | `/api/dashboard/overview/` | 仪表盘总览 | IsAuthenticated |
| GET | `/api/dashboard/recent_out_assets/` | 最近出库记录 | IsAuthenticated |
| GET | `/api/dashboard/recent_recycle_assets/` | 最近回收记录 | IsAuthenticated |

---

## 五、请求参数与请求体

### 5.1 认证管理模块

#### POST /api/auth/register/ -- 用户注册

**权限**: AllowAny

**请求体**:

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `auth_username` | string | 是 | 用户名 |
| `password` | string | 是 | 密码（write_only），需通过 Django validate_password 验证 |
| `password2` | string | 是 | 确认密码（write_only），必须与 password 一致 |
| `email` | string | 否 | 邮箱 |
| `auth_phone` | string | 否 | 手机号 |

**请求体示例**:

```json
{
  "auth_username": "zhangsan",
  "password": "StrongP@ssw0rd!",
  "password2": "StrongP@ssw0rd!",
  "email": "zhangsan@example.com",
  "auth_phone": "13800138000"
}
```

**业务规则**:
- 注册强制 `auth_is_staff=False`
- `password` 需通过 Django `validate_password` 验证
- 成功返回 201 状态码

---

#### POST /api/auth/login/ -- 用户登录

**权限**: AllowAny

**请求体**:

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `auth_username` | string | 是 | 用户名 |
| `password` | string | 是 | 密码（write_only） |

**请求体示例**:

```json
{
  "auth_username": "zhangsan",
  "password": "StrongP@ssw0rd!"
}
```

---

#### POST /api/auth/logout/ -- 退出登录

**权限**: IsAuthenticated

**请求体**:

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `refresh` | string | 是 | 要作废的 refresh token |

**请求体示例**:

```json
{
  "refresh": "eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9..."
}
```

---

#### GET /api/auth/profile/ -- 获取当前用户信息

**权限**: IsAuthenticated

**请求参数**: 无

---

#### PUT /api/auth/profile/ -- 更新当前用户信息

**权限**: IsAuthenticated

**请求体**:

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `email` | string | 否 | 邮箱 |
| `auth_phone` | string | 否 | 手机号 |
| `password` | string | 否 | 新密码（write_only），需通过 validate_password 验证 |

**请求体示例**:

```json
{
  "email": "newemail@example.com",
  "auth_phone": "13900139000"
}
```

---

#### POST /api/auth/token/ -- 获取 Token（SimpleJWT 标准）

**权限**: AllowAny

**请求体**:

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `username` | string | 是 | 用户名 |
| `password` | string | 是 | 密码 |

---

#### POST /api/auth/token/refresh/ -- 刷新 Token

**权限**: AllowAny

**请求体**:

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `refresh` | string | 是 | refresh token |

---

#### POST /api/auth/token/verify/ -- 验证 Token

**权限**: AllowAny

**请求体**:

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `token` | string | 是 | access token |

---

#### GET /api/auth/users/list_active/ -- 获取所有激活用户

**权限**: IsAdminUser

**请求参数**: 无

**说明**: 返回所有 `auth_is_active=True` 的用户列表。

---

### 5.2 部门管理

#### GET /api/users/departments/ -- 部门列表

**权限**: IsAuthenticated

**查询参数**:

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `page` | int | 否 | 页码 |
| `page_size` | int | 否 | 每页数量（最大 100） |
| `search` | string | 否 | 搜索关键词（匹配 department_name、department_code） |
| `ordering` | string | 否 | 排序字段（department_code、department_name），前缀 `-` 降序 |

---

#### POST /api/users/departments/ -- 创建部门

**权限**: IsAdminUser

**请求体**:

| 字段 | 类型 | 必填 | 约束 | 说明 |
|------|------|------|------|------|
| `department_code` | string | 是 | max_length=20, unique | 部门编码 |
| `department_name` | string | 是 | max_length=100, unique | 部门名称 |
| `department_information` | string | 否 | max_length=20 | 部门信息员 |
| `parent_code` | string | 否 | max_length=20 | 上级部门编码 |
| `level` | int | 否 | default=0 | 层级 |
| `sort_order` | int | 否 | default=0 | 排序 |

**请求体示例**:

```json
{
  "department_code": "D001",
  "department_name": "技术部",
  "department_information": "张三",
  "parent_code": null,
  "level": 0,
  "sort_order": 1
}
```

---

#### GET /api/users/departments/{department_code}/ -- 部门详情

**权限**: IsAuthenticated

**路径参数**:

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `department_code` | string | 是 | 部门编码 |

---

#### PUT /api/users/departments/{department_code}/ -- 全量更新部门

**权限**: IsAdminUser

**路径参数**: 同上

**请求体**: 同创建部门（所有字段均需传递）

---

#### PATCH /api/users/departments/{department_code}/ -- 部分更新部门

**权限**: IsAdminUser

**路径参数**: 同上

**请求体**: 同创建部门（仅传递需要修改的字段）

---

#### DELETE /api/users/departments/{department_code}/ -- 删除部门

**权限**: IsAdminUser

**路径参数**: 同上

**说明**: 软删除，不会物理删除数据库记录。

---

#### GET /api/users/departments/{department_code}/employees/ -- 获取部门下的员工

**权限**: IsAuthenticated

**路径参数**:

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `department_code` | string | 是 | 部门编码 |

---

#### GET /api/users/departments/tree/ -- 获取部门树形结构

**权限**: IsAuthenticated

**查询参数**: 无

**说明**: 返回递归嵌套的部门树，每个节点包含 `children`（子部门列表）和 `employee_count`（当前部门员工数量）。

---

#### GET /api/users/departments/{department_code}/children/ -- 获取子部门

**权限**: IsAuthenticated

**路径参数**:

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `department_code` | string | 是 | 部门编码 |

---

#### GET /api/users/departments/{department_code}/path/ -- 获取部门路径（面包屑导航）

**权限**: IsAuthenticated

**路径参数**:

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `department_code` | string | 是 | 部门编码 |

**说明**: 返回从根部门到当前部门的完整路径，用于面包屑导航。

---

#### GET /api/users/departments/{department_code}/descendants/ -- 获取所有后代部门

**权限**: IsAuthenticated

**路径参数**:

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `department_code` | string | 是 | 部门编码 |

**说明**: 返回当前部门的所有后代部门（递归），不包含当前部门自身。

---

#### PUT /api/users/departments/{department_code}/move/ -- 移动部门

**权限**: IsAdminUser

**路径参数**:

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `department_code` | string | 是 | 部门编码 |

**请求体**:

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `target_parent_code` | string/null | 是 | 目标父部门编码，`null` 表示成为根部门 |

**请求体示例**:

```json
{
  "target_parent_code": "D001"
}
```

**业务规则**:
- 自动验证循环引用（不能移动到自己的子部门下）
- 层级约束：移动后不超过 6 层

---

#### PUT /api/users/departments/sort/ -- 批量排序部门

**权限**: IsAdminUser

**请求体**:

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `items` | array | 是 | 排序项列表 |

**items 子项**:

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `department_code` | string | 是 | 部门编码 |
| `sort_order` | int | 是 | 排序值 |

**请求体示例**:

```json
{
  "items": [
    {"department_code": "D001", "sort_order": 1},
    {"department_code": "D002", "sort_order": 2},
    {"department_code": "D003", "sort_order": 3}
  ]
}
```

---

### 5.3 员工管理

#### GET /api/users/employees/ -- 员工列表

**权限**: IsAuthenticated

**查询参数**:

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `page` | int | 否 | 页码 |
| `page_size` | int | 否 | 每页数量（最大 100） |
| `employee_status` | string | 否 | 员工状态：active/left/retirement |
| `employee_department__department_code` | string | 否 | 部门编码 |
| `search` | string | 否 | 搜索关键词（匹配 employee_name、employee_jobcode、employee_phone） |
| `ordering` | string | 否 | 排序字段（employee_jobcode、employee_name），前缀 `-` 降序 |

---

#### POST /api/users/employees/ -- 创建员工

**权限**: IsAdminUser

**请求体**:

| 字段 | 类型 | 必填 | 约束 | 说明 |
|------|------|------|------|------|
| `employee_jobcode` | string | 是 | max_length=20, unique | 工号 |
| `employee_name` | string | 是 | max_length=100 | 姓名 |
| `employee_status` | string | 否 | default=active | 状态（active/left/retirement） |
| `employee_department` | string | 否 | 部门编码 | 所属部门 |
| `employee_phone` | string | 否 | max_length=15, unique | 手机号 |
| `employee_location` | string | 否 | max_length=100 | 位置 |
| `employee_description` | text | 否 | | 描述 |
| `sort_order` | int | 否 | default=0 | 排序 |

**请求体示例**:

```json
{
  "employee_jobcode": "E001",
  "employee_name": "张三",
  "employee_status": "active",
  "employee_department": "D001",
  "employee_phone": "13800138000",
  "employee_location": "北京总部",
  "employee_description": "技术部开发工程师"
}
```

---

#### GET /api/users/employees/{employee_jobcode}/ -- 员工详情

**权限**: IsAuthenticated

**路径参数**:

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `employee_jobcode` | string | 是 | 工号 |

---

#### PUT/PATCH /api/users/employees/{employee_jobcode}/ -- 更新员工

**权限**: IsAdminUser

**路径参数**: 同上

**请求体**: 同创建员工（PUT 需全部字段，PATCH 仅需修改字段）

---

#### DELETE /api/users/employees/{employee_jobcode}/ -- 删除员工

**权限**: IsAdminUser

**路径参数**: 同上

**说明**: 软删除。

---

#### GET /api/users/employees/search/ -- 全局搜索员工

**权限**: IsAuthenticated

**查询参数**:

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `keyword` | string | 是 | 搜索关键词 |
| `page` | int | 否 | 页码 |
| `page_size` | int | 否 | 每页数量 |

**搜索范围**: 姓名、工号、手机号、描述、部门名称、状态别名（如"在职"匹配 active、"离职"匹配 left、"退休"匹配 retirement）。

---

#### GET /api/users/employees/statistics/ -- 员工统计

**权限**: IsAuthenticated

**查询参数**: 无

**返回字段**:

| 字段 | 说明 |
|------|------|
| `total_employees` | 员工总数 |
| `active_employees` | 在职员工数 |
| `by_status` | 按状态分组统计 |
| `by_department` | 按部门分组统计 |

---

#### GET /api/users/employees/active_employees/ -- 在职员工列表

**权限**: IsAuthenticated

**查询参数**: 支持分页（page、page_size）

---

#### POST /api/users/employees/{employee_jobcode}/change_status/ -- 更改员工状态

**权限**: IsAdminUser

**路径参数**:

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `employee_jobcode` | string | 是 | 工号 |

**请求体**:

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `status` | string | 是 | 目标状态：active/left/retirement |

**请求体示例**:

```json
{
  "status": "left"
}
```

---

#### PUT /api/users/employees/sort/ -- 批量更新员工排序

**权限**: IsAdminUser

**请求体**:

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `items` | array | 是 | 排序项列表 |

**items 子项**:

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `employee_jobcode` | string | 是 | 员工工号 |
| `sort_order` | int | 是 | 排序值 |

**请求体示例**:

```json
[
  {"employee_jobcode": "E001", "sort_order": 1},
  {"employee_jobcode": "E002", "sort_order": 2},
  {"employee_jobcode": "E003", "sort_order": 3}
]
```

---

### 5.4 仓库管理

#### GET /api/assets/storages/ -- 仓库列表

**权限**: IsAuthenticated

**查询参数**:

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `page` | int | 否 | 页码 |
| `page_size` | int | 否 | 每页数量 |
| `storage_type` | string | 否 | 仓库类型：newasset/recycle/damaged |
| `keyword` | string | 否 | 搜索关键词（匹配编码、名称、地址） |
| `ordering` | string | 否 | 排序字段（storage_code、storage_name） |

---

#### POST /api/assets/storages/ -- 创建仓库

**权限**: IsAuthenticated

**请求体**:

| 字段 | 类型 | 必填 | 约束 | 说明 |
|------|------|------|------|------|
| `storage_code` | string | 是 | max_length=20, unique | 仓库编码 |
| `storage_name` | string | 是 | max_length=100, unique | 仓库名称 |
| `storage_address` | string | 否 | max_length=200 | 地址 |
| `storage_type` | string | 否 | default=newasset | 类型（newasset/recycle/damaged） |
| `storage_description` | text | 否 | | 描述 |

**请求体示例**:

```json
{
  "storage_code": "WH001",
  "storage_name": "北京新货仓库",
  "storage_address": "北京市朝阳区XX路XX号",
  "storage_type": "newasset",
  "storage_description": "存放新采购的资产"
}
```

---

#### GET /api/assets/storages/{storage_code}/ -- 仓库详情

**权限**: IsAuthenticated

**路径参数**:

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `storage_code` | string | 是 | 仓库编码 |

---

#### PUT/PATCH /api/assets/storages/{storage_code}/ -- 更新仓库

**权限**: IsAuthenticated

**路径参数**: 同上

---

#### DELETE /api/assets/storages/{storage_code}/ -- 删除仓库

**权限**: IsAuthenticated

**路径参数**: 同上

**说明**: 软删除。

---

#### GET /api/assets/storages/statistics/ -- 仓库统计

**权限**: IsAuthenticated

**返回字段**:

| 字段 | 说明 |
|------|------|
| `total_storages` | 仓库总数 |
| `by_type` | 按类型分组统计 |

---

### 5.5 资产类型管理

#### GET /api/assets/asset-types/ -- 资产类型列表

**权限**: IsAuthenticated

**查询参数**:

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `page` | int | 否 | 页码 |
| `page_size` | int | 否 | 每页数量 |
| `asset_type_category` | string | 否 | 大类：hardware/software/other/lowvalue |
| `search` | string | 否 | 搜索关键词（匹配 asset_type_secondary、asset_type_code） |
| `ordering` | string | 否 | 排序字段（asset_type_code、asset_type_secondary） |

---

#### POST /api/assets/asset-types/ -- 创建资产类型

**权限**: IsAuthenticated

**请求体**:

| 字段 | 类型 | 必填 | 约束 | 说明 |
|------|------|------|------|------|
| `asset_type_code` | string | 是 | max_length=20, unique | 类型编码 |
| `asset_type_secondary` | string | 是 | max_length=100 | 二级分类 |
| `asset_type_primary` | string | 否 | max_length=100 | 一级分类 |
| `asset_type_category` | string | 否 | default=hardware | 大类（hardware/software/other/lowvalue） |
| `asset_type_description` | text | 否 | | 描述 |

**请求体示例**:

```json
{
  "asset_type_code": "AT001",
  "asset_type_secondary": "笔记本电脑",
  "asset_type_primary": "计算机设备",
  "asset_type_category": "hardware",
  "asset_type_description": "各类笔记本电脑"
}
```

---

#### GET /api/assets/asset-types/{asset_type_code}/ -- 资产类型详情

**权限**: IsAuthenticated

**路径参数**:

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `asset_type_code` | string | 是 | 类型编码 |

---

#### PUT/PATCH /api/assets/asset-types/{asset_type_code}/ -- 更新资产类型

**权限**: IsAuthenticated

**路径参数**: 同上

---

#### DELETE /api/assets/asset-types/{asset_type_code}/ -- 删除资产类型

**权限**: IsAuthenticated

**路径参数**: 同上

---

### 5.6 合同管理

#### GET /api/assets/contracts/ -- 合同列表

**权限**: IsAuthenticated

**查询参数**:

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `page` | int | 否 | 页码 |
| `page_size` | int | 否 | 每页数量 |
| `contract_type` | string | 否 | 合同类型 |
| `contract_settlment_status` | string | 否 | 结算状态：pending/settled |
| `ordering` | string | 否 | 排序字段（contract_code、contract_signing_date、contract_price） |

**默认排序**: `-contract_signing_date`（签订日期降序）

---

#### POST /api/assets/contracts/ -- 创建合同

**权限**: IsAuthenticated

**请求体**:

| 字段 | 类型 | 必填 | 约束 | 说明 |
|------|------|------|------|------|
| `contract_code` | string | 是 | max_length=20, unique | 合同编码 |
| `contract_name` | string | 是 | max_length=100, unique | 合同名称 |
| `contract_type` | string | 否 | default=purchase | 类型（purchase/service/information_construction/direct_procurement） |
| `contract_price` | decimal(10,2) | 是 | | 合同金额 |
| `contract_supplier` | string | 是 | max_length=100 | 供应商 |
| `contract_signing_date` | date | 是 | | 签订日期（格式：YYYY-MM-DD） |
| `contract_warranty_period` | int | 否 | default=0 | 保修期（年） |
| `contract_preliminary_acceptance_date` | date | 否 | | 初验日期 |
| `contract_final_acceptance_date` | date | 否 | | 终验日期 |
| `contract_settlment_status` | string | 否 | default=pending | 结算状态（pending/settled） |
| `contract_settlment_price` | decimal(10,2) | 否 | | 结算金额 |
| `contract_paid_count_number` | int | 否 | default=0 | 已付次数 |
| `contract_paid_price` | decimal(10,2) | 否 | | 已付金额 |
| `contract_paid_record` | text | 否 | | 付款记录 |

**请求体示例**:

```json
{
  "contract_code": "CT001",
  "contract_name": "2026年办公设备采购合同",
  "contract_type": "purchase",
  "contract_price": "500000.00",
  "contract_supplier": "XX科技有限公司",
  "contract_signing_date": "2026-01-15",
  "contract_warranty_period": 3,
  "contract_settlment_status": "pending"
}
```

---

#### GET /api/assets/contracts/{contract_code}/ -- 合同详情

**权限**: IsAuthenticated

**路径参数**:

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `contract_code` | string | 是 | 合同编码 |

---

#### PUT/PATCH /api/assets/contracts/{contract_code}/ -- 更新合同

**权限**: IsAuthenticated

**路径参数**: 同上

---

#### DELETE /api/assets/contracts/{contract_code}/ -- 删除合同

**权限**: IsAuthenticated

**路径参数**: 同上

**业务规则**: 软删除。删除前会验证是否有关联资产，如有关联资产则拒绝删除。

---

#### GET /api/assets/contracts/search/ -- 全局搜索合同

**权限**: IsAuthenticated

**查询参数**:

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `keyword` | string | 是 | 搜索关键词 |
| `page` | int | 否 | 页码 |
| `page_size` | int | 否 | 每页数量 |

**搜索范围**: contract_code、contract_name、contract_supplier

---

#### GET /api/assets/contracts/getcontractByname/{name}/ -- 按名称搜索合同

**权限**: IsAuthenticated

**路径参数**:

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `name` | string | 是 | 合同名称（模糊匹配） |

---

#### GET /api/assets/contracts/statistics/ -- 合同统计

**权限**: IsAuthenticated

**查询参数**: 无

---

#### POST /api/assets/contracts/{contract_code}/payment_record/ -- 添加付款记录

**权限**: IsAuthenticated

**路径参数**:

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `contract_code` | string | 是 | 合同编码 |

**请求体**:

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `amount` | decimal | 是 | 付款金额 |
| `description` | string | 否 | 付款说明 |

**请求体示例**:

```json
{
  "amount": "100000.00",
  "description": "第一期付款"
}
```

---

#### POST /api/assets/contracts/{contract_code}/update_settlement_status/ -- 更新合同结算状态

**权限**: IsAuthenticated

**路径参数**:

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `contract_code` | string | 是 | 合同编码 |

**请求体**:

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `status` | string | 是 | 结算状态（pending/settled） |

**请求体示例**:

```json
{
  "status": "settled"
}
```

---

### 5.7 资产管理

#### GET /api/assets/assets/ -- 资产列表

**权限**: IsAuthenticated

**查询参数**:

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `page` | int | 否 | 页码 |
| `page_size` | int | 否 | 每页数量 |
| `asset_current_status` | string | 否 | 资产状态（in_store/recycled_pending/in_use/damaged/scrapped） |
| `asset_type_code` | string | 否 | 资产类型编码 |
| `asset_storage_code` | string | 否 | 仓库编码 |
| `keyword` | string | 否 | 搜索关键词（匹配编码、名称、品牌、规格） |
| `ordering` | string | 否 | 排序字段（asset_code、asset_entry_date、asset_purchase_price、asset_name） |

**默认排序**: `-asset_entry_date`（入库日期降序）

**过滤字段**: `asset_current_status`、`asset_type_code`、`asset_storage_code`

---

#### POST /api/assets/assets/ -- 创建资产（支持批量）

**权限**: IsAuthenticated

**请求体**:

| 字段 | 类型 | 必填 | 约束 | 说明 |
|------|------|------|------|------|
| `asset_code` | string | 否 | read_only | 资产编码（后端自动生成，创建后不可修改） |
| `asset_name` | string | 是 | max_length=100, unique | 资产名称 |
| `asset_purchase_price` | decimal(10,2) | 是 | | 采购价格 |
| `asset_purchase_date` | date | 是 | | 购买日期 |
| `asset_entry_date` | date | 是 | | 入库日期 |
| `asset_type_code` | string | 是 | | 资产类型编码 |
| `asset_contract_code` | string | 否 | | 合同编码 |
| `asset_storage_code` | string | 否 | | 仓库编码 |
| `asset_entry_person_jobcode` | string | 否 | | 入库人工号 |
| `asset_applicant_jobcode` | string | 否 | | 申请人工号 |
| `asset_manager_jobcode` | string | 否 | | 管理人工号 |
| `asset_brand` | string | 否 | max_length=100 | 品牌 |
| `asset_specification` | string | 否 | max_length=100 | 规格 |
| `asset_unit` | string | 否 | max_length=50 | 单位 |
| `asset_warranty_period` | int | 否 | | 保修期（年） |
| `asset_description` | text | 否 | | 描述 |

> **v1.1.0 变更**: `asset_code` 改为 `read_only`，由后端自动生成。格式为 `ASSET-{category}-{type_code}-{YYYYMMDD}-{random}-{seq}`。支持批量创建（传入数组）。

**单条创建请求体示例**:

```json
{
  "asset_name": "ThinkPad X1 Carbon",
  "asset_purchase_price": "8999.00",
  "asset_purchase_date": "2026-01-10",
  "asset_entry_date": "2026-01-15",
  "asset_type_code": "AT001",
  "asset_contract_code": "CT001",
  "asset_storage_code": "WH001",
  "asset_entry_person_jobcode": "E001",
  "asset_brand": "Lenovo",
  "asset_specification": "i7/16G/512G SSD",
  "asset_unit": "台",
  "asset_warranty_period": 3
}
```

**批量创建请求体示例**:

```json
[
  {
    "asset_name": "ThinkPad X1 Carbon",
    "asset_purchase_price": "8999.00",
    "asset_purchase_date": "2026-01-10",
    "asset_entry_date": "2026-01-15",
    "asset_type_code": "AT001",
    "asset_storage_code": "WH001"
  },
  {
    "asset_name": "Dell U2723QE",
    "asset_purchase_price": "3999.00",
    "asset_purchase_date": "2026-01-10",
    "asset_entry_date": "2026-01-15",
    "asset_type_code": "AT002",
    "asset_storage_code": "WH001"
  }
]
```

**业务规则**:
- 创建时自动生成 `asset_recordcode`（格式：`Entry` + `YYYYMMDDHHmmss` + 8 位 UUID）
- 创建时自动生成 `asset_code`（格式：`ASSET-{category}-{type_code}-{YYYYMMDD}-{random}-{seq}`）
- 成功返回 201 状态码

---

#### GET /api/assets/assets/{asset_code}/ -- 资产详情

**权限**: IsAuthenticated

**路径参数**:

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `asset_code` | string | 是 | 资产编码 |

**说明**: 返回资产详情，包含嵌套的关联对象（资产类型、合同、仓库、入库人、申请人、管理人）。

---

#### PUT/PATCH /api/assets/assets/{asset_code}/ -- 更新资产

**权限**: IsAuthenticated

**路径参数**: 同上

**说明**: `asset_code` 创建后不可修改。

---

#### DELETE /api/assets/assets/{asset_code}/ -- 删除资产

**权限**: IsAuthenticated

**路径参数**: 同上

**说明**: 软删除。

---

#### GET /api/assets/assets/getassetbyname/{name}/ -- 按名称搜索资产

**权限**: IsAuthenticated

**路径参数**:

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `name` | string | 是 | 资产名称（模糊匹配） |

---

#### GET /api/assets/assets/search/ -- 搜索资产

**权限**: IsAuthenticated

**查询参数**: 同资产列表（GET /api/assets/assets/）

---

#### GET /api/assets/assets/statistics/ -- 资产统计

**权限**: IsAuthenticated

**查询参数**: 无

---

#### GET /api/assets/assets/search_available/ -- 获取可用资产

**权限**: IsAuthenticated

**查询参数**: 无

**说明**: 返回状态为 `in_store`（在库）和 `recycled_pending`（已回收待发放）的资产。

---

#### POST /api/assets/assets/{asset_code}/change_status/ -- 变更资产状态

**权限**: IsAuthenticated

**路径参数**:

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `asset_code` | string | 是 | 资产编码 |

**请求体**:

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `status` | string | 是 | 目标状态 |
| `description` | string | 否 | 变更说明 |

**请求体示例**:

```json
{
  "status": "in_use",
  "description": "已出库给张三使用"
}
```

---

#### GET /api/assets/assets/combined_details/ -- 资产+合同组合详情

**权限**: IsAuthenticated

**查询参数**:

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `asset_code` | string | 是 | 资产编码 |

---

#### GET /api/assets/assets/getassetbyrecordcode/{asset_recordcode}/ -- 按入库记录编码搜索资产

**权限**: IsAuthenticated

**路径参数**:

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `asset_recordcode` | string | 是 | 入库记录编码 |

---

#### GET /api/assets/assets/contract_by_asset/{asset_code}/ -- 通过资产编码查询关联合同

**权限**: IsAuthenticated

**路径参数**:

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `asset_code` | string | 是 | 资产编码 |

---

### 5.8 出库管理

#### GET /api/assets/out-assets/ -- 出库列表

**权限**: IsAuthenticated

**查询参数**:

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `page` | int | 否 | 页码 |
| `page_size` | int | 否 | 每页数量 |
| `outasset_type` | string | 否 | 出库类型（receive/borrow） |
| `outasset_applicant_jobcode` | string | 否 | 申请人工号 |
| `outasset_manager_jobcode` | string | 否 | 管理人工号 |
| `keyword` | string | 否 | 搜索关键词 |
| `searchType` | string | 否 | 搜索类型（asset/user/all） |
| `outasset_current_status` | string | 否 | 出库状态（in_use/recycled_pending/damaged/scrapped） |
| `ordering` | string | 否 | 排序字段 |

**默认排序**: `-outasset_date`（出库日期降序）

---

#### POST /api/assets/out-assets/ -- 创建出库记录

**权限**: IsAuthenticated

**请求体**:

| 字段 | 类型 | 必填 | 约束 | 说明 |
|------|------|------|------|------|
| `outasset_code` | string | 是 | | 资产编码 |
| `outasset_number` | int | 否 | default=1 | 出库数量 |
| `outasset_applicant_jobcode` | string | 否 | read_only | 申请人工号（通过 Asset FK 关联自动获取） |
| `outasset_manager_jobcode` | string | 是 | | 管理人工号 |
| `outasset_using_location` | string | 否 | read_only | 使用地点（通过 Asset FK 关联自动获取） |
| `outasset_type` | string | 否 | default=receive | 类型（receive/borrow） |
| `return_date` | date | 否 | | 归还日期 |
| `outasset_previous_status` | string | 否 | read_only | 出库前资产状态（自动记录） |
| `outasset_description` | text | 否 | | 描述 |
| `outasset_date` | date | 否 | default=当天 | 出库日期 |

> **v1.1.0 变更**: `outasset_applicant_jobcode`、`outasset_manager_jobcode`、`outasset_using_location` 改为只读字段（通过 Asset FK 关联自动获取）。新增 `return_date`（归还日期）和 `outasset_previous_status`（出库前资产状态，只读）字段。

**请求体示例**:

```json
{
  "outasset_code": "A001",
  "outasset_number": 1,
  "outasset_manager_jobcode": "E002",
  "outasset_type": "receive",
  "return_date": "2027-01-15",
  "outasset_description": "日常办公使用"
}
```

**业务规则**:
- 自动生成 `outasset_recordcode`（格式：`OUT-YYYYMMDD-XXXXXXXX`）
- 出库后资产状态自动变为 `in_use`
- 成功返回 201 状态码

---

#### GET /api/assets/out-assets/{outasset_recordcode}/ -- 出库详情

**权限**: IsAuthenticated

**路径参数**:

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `outasset_recordcode` | string | 是 | 出库记录编码 |

---

#### PUT/PATCH /api/assets/out-assets/{outasset_recordcode}/ -- 更新出库记录

**权限**: IsAuthenticated

**路径参数**: 同上

---

#### DELETE /api/assets/out-assets/{outasset_recordcode}/ -- 删除出库记录

**权限**: IsAuthenticated

**路径参数**: 同上

---

#### GET /api/assets/out-assets/recyclable/ -- 可回收资产列表

**权限**: IsAuthenticated

**查询参数**:

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `page` | int | 否 | 页码 |
| `page_size` | int | 否 | 每页数量 |
| `years` | int | 否 | 出库时长（年），返回出库满 N 年及以上的记录 |
| `search` | string | 否 | 搜索关键词 |
| `searchType` | string | 否 | 搜索类型（asset/user/all） |
| `asset_code` | string | 否 | 资产编码 |
| `employee_jobcode` | string | 否 | 员工工号 |
| `department_code` | string | 否 | 部门编码 |
| `ordering` | string | 否 | 排序字段 |

**筛选条件**: `outasset_current_status=in_use`

---

#### GET /api/assets/out-assets/by-asset/{asset_code}/ -- 按资产查出库记录

**权限**: IsAuthenticated

**路径参数**:

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `asset_code` | string | 是 | 资产编码 |

---

#### GET /api/assets/out-assets/by-applicant/{applicant_jobcode}/ -- 按申请人工号查出库记录

**权限**: IsAuthenticated

**路径参数**:

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `applicant_jobcode` | string | 是 | 申请人工号 |

---

#### GET /api/assets/out-assets/statistics/ -- 出库统计

**权限**: IsAuthenticated

**查询参数**: 无

---

### 5.9 回收管理

#### GET /api/assets/recycle-assets/ -- 回收列表

**权限**: IsAuthenticated

**查询参数**:

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `page` | int | 否 | 页码 |
| `page_size` | int | 否 | 每页数量 |
| `recycle_asset_storage_code` | string | 否 | 回收仓库编码 |
| `recycle_asset_code` | string | 否 | 资产编码 |
| `recycle_asset_recycle_person_jobcode` | string | 否 | 回收人工号 |
| `recycle_date_from` | date | 否 | 回收日期起始（YYYY-MM-DD） |
| `recycle_date_to` | date | 否 | 回收日期截止（YYYY-MM-DD） |
| `search` | string | 否 | 搜索关键词（匹配资产名称、仓库名称） |

**默认排序**: `-recycle_asset_date`（回收日期降序）

---

#### POST /api/assets/recycle-assets/ -- 创建回收记录

**权限**: IsAuthenticated

**请求体**:

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `outasset_recordcode` | string | 是 | 出库记录编码 |
| `recycle_asset_storage_code` | string | 否 | read_only | 回收仓库编码（通过出库记录关联自动获取） |
| `recycle_asset_recycle_person_jobcode` | string | 否 | read_only | 回收人工号（通过出库记录关联自动获取） |
| `recycle_asset_date` | date | 是 | 回收日期 |
| `recycle_asset_number` | int | 否 | default=1，回收数量 |
| `recycle_asset_description` | text | 否 | 描述 |
| `operator_jobcode` | string | 否 | 操作人工号 |
| `recycle_record_code` | string | 否 | read_only | 回收记录编码（自动生成） |

> **v1.1.0 变更**: `recycle_asset_storage_code`、`recycle_asset_recycle_person_jobcode` 改为只读字段（通过出库记录关联自动获取）。新增 `operator_jobcode`（操作人工号）和 `recycle_record_code`（回收记录编码，自动生成，格式：`RECYCLE-YYYYMMDD-XXXXXXXX`）字段。

**请求体示例**:

```json
{
  "outasset_recordcode": "OUT-20260115-XXXXXXXX",
  "recycle_asset_date": "2026-05-20",
  "operator_jobcode": "E002",
  "recycle_asset_description": "员工离职回收"
}
```

**业务规则**:
- 回收后资产状态自动变为 `recycled_pending`
- 自动生成 `recycle_record_code`（格式：`RECYCLE-YYYYMMDD-XXXXXXXX`）
- 成功返回 201 状态码

---

#### GET /api/assets/recycle-assets/{outasset_recordcode}/ -- 回收详情

**权限**: IsAuthenticated

**路径参数**:

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `outasset_recordcode` | string | 是 | 出库记录编码 |

---

#### PUT/PATCH /api/assets/recycle-assets/{outasset_recordcode}/ -- 更新回收记录

**权限**: IsAuthenticated

**路径参数**: 同上

---

#### DELETE /api/assets/recycle-assets/{outasset_recordcode}/ -- 删除回收记录

**权限**: IsAuthenticated

**路径参数**: 同上

---

#### GET /api/assets/recycle-assets/by-asset/{asset_code}/ -- 按资产查回收记录

**权限**: IsAuthenticated

**路径参数**:

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `asset_code` | string | 是 | 资产编码 |

---

#### GET /api/assets/recycle-assets/by-outasset/{outasset_recordcode}/ -- 通过出库记录编码查询回收记录

**权限**: IsAuthenticated

**路径参数**:

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `outasset_recordcode` | string | 是 | 出库记录编码 |

---

### 5.10 待报废管理

#### GET /api/assets/damaged-assets/ -- 待报废列表

**权限**: IsAuthenticated

**查询参数**:

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `page` | int | 否 | 页码 |
| `page_size` | int | 否 | 每页数量 |
| `damaged_asset_storage_code` | string | 否 | 仓库编码 |
| `approval_status` | string | 否 | 审批状态（pending/approved/rejected） |
| `search` | string | 否 | 搜索关键词（匹配资产名称） |

**默认排序**: `-damaged_date`（申请日期降序）

---

#### POST /api/assets/damaged-assets/ -- 创建待报废申请

**权限**: IsAuthenticated

**请求体**:

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `damaged_asset_code` | string | 是 | 资产编码 |
| `damaged_asset_contract_code` | string | 否 | read_only | 合同编码（通过 Asset FK 关联自动获取） |
| `damaged_asset_storage_code` | string | 否 | read_only | 仓库编码（通过 Asset FK 关联自动获取） |
| `damaged_asset_number` | int | 否 | default=1，数量 |
| `damaged_date` | date | 否 | default=当天，申请日期 |
| `approval_status` | string | 否 | default=pending，审批状态 |
| `approver` | string | 否 | 审批人工号 |
| `damaged_asset_description` | text | 否 | 描述 |

> **v1.1.0 变更**: `damaged_asset_contract_code`、`damaged_asset_storage_code` 改为只读字段（通过 Asset FK 关联自动获取）。

**请求体示例**:

```json
{
  "damaged_asset_code": "A001",
  "damaged_asset_description": "设备老化严重，无法继续使用"
}
```

---

#### GET /api/assets/damaged-assets/{damaged_asset_code}/ -- 待报废详情

**权限**: IsAuthenticated

**路径参数**:

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `damaged_asset_code` | string | 是 | 资产编码 |

---

#### PUT/PATCH /api/assets/damaged-assets/{damaged_asset_code}/ -- 更新待报废申请

**权限**: IsAuthenticated

**路径参数**: 同上

---

#### DELETE /api/assets/damaged-assets/{damaged_asset_code}/ -- 取消待报废申请

**权限**: IsAuthenticated

**路径参数**: 同上

**业务规则**: 取消待报废申请（软删除待报废记录），恢复资产状态。

---

#### POST /api/assets/damaged-assets/{damaged_asset_code}/approve/ -- 审批通过

**权限**: IsAuthenticated

**路径参数**:

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `damaged_asset_code` | string | 是 | 资产编码 |

**请求体**:

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `approver_jobcode` | string | 否 | 审批人工号（默认当前用户） |
| `operator_name` | string | 否 | 操作人姓名（默认当前用户） |

**请求体示例**:

```json
{
  "approver_jobcode": "E003",
  "operator_name": "李四"
}
```

**业务规则**:
- 审批通过后自动创建已报废记录
- 资产状态自动变为 `scrapped`

---

#### POST /api/assets/damaged-assets/{damaged_asset_code}/reject/ -- 审批拒绝

**权限**: IsAuthenticated

**路径参数**: 同上

**请求体**: 同审批通过

**业务规则**:
- 审批拒绝后资产状态恢复为 `recycled_pending`

---

#### GET /api/assets/damaged-assets/by-asset/{asset_code}/ -- 按资产查待报废记录

**权限**: IsAuthenticated

**路径参数**:

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `asset_code` | string | 是 | 资产编码 |

---

### 5.11 已报废管理

#### GET /api/assets/waste-assets/ -- 已报废列表

**权限**: IsAuthenticated

**查询参数**:

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `page` | int | 否 | 页码 |
| `page_size` | int | 否 | 每页数量 |
| `waste_asset_date` | date | 否 | 报废日期 |
| `search` | string | 否 | 搜索关键词（匹配资产名称、资产编码） |

**默认排序**: `-waste_asset_date`（报废日期降序）

**lookup_field**: `waste_asset_code__asset_code`（通过资产编码查找）

---

#### GET /api/assets/waste-assets/{asset_code}/ -- 已报废详情

**权限**: IsAuthenticated

**路径参数**:

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `asset_code` | string | 是 | 资产编码 |

---

#### GET /api/assets/waste-assets/by-asset/{asset_code}/ -- 按资产查已报废记录

**权限**: IsAuthenticated

**路径参数**: 同上

---

#### GET /api/assets/waste-assets/statistics/ -- 已报废统计

**权限**: IsAuthenticated

**查询参数**: 无

**返回字段**:

| 字段 | 说明 |
|------|------|
| `total_waste_assets` | 已报废总数 |
| `this_year_waste` | 本年度报废数量 |
| `monthly_waste` | 本年度每月报废数量（1-12月） |

---

#### GET /api/assets/waste-assets/by-date-range/ -- 按日期范围查询已报废记录

**权限**: IsAuthenticated

**查询参数**:

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `start_date` | date | 是 | 起始日期（格式：YYYY-MM-DD） |
| `end_date` | date | 是 | 结束日期（格式：YYYY-MM-DD） |

---

### 5.12 硬盘序列号管理

#### GET /api/assets/harddisk-sn/ -- 硬盘序列号列表

**权限**: IsAuthenticated

**查询参数**:

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `page` | int | 否 | 页码 |
| `page_size` | int | 否 | 每页数量 |
| `harddisk_status` | string | 否 | 硬盘状态（active/repair/scrap/lost/damaged） |
| `search` | string | 否 | 搜索关键词（匹配 harddisk_sn_code、harddisk_sn_description） |
| `ordering` | string | 否 | 排序字段（id、harddisk_number） |

**默认排序**: `-id`

---

#### POST /api/assets/harddisk-sn/ -- 创建硬盘序列号

**权限**: IsAuthenticated

**请求体**:

| 字段 | 类型 | 必填 | 约束 | 说明 |
|------|------|------|------|------|
| `asset_code` | string | 是 | | 资产编码 |
| `harddisk_number` | int | 是 | | 硬盘数量 |
| `harddisk_sn_code` | string | 是 | max_length=100, unique | 序列号 |
| `harddisk_no` | int | 否 | default=1 | 硬盘编号 |
| `harddisk_type` | string | 否 | default=HDD | 类型（HDD/SSD/NVMe/Other） |
| `harddisk_sn_description` | text | 否 | | 描述 |
| `harddisk_status` | string | 否 | default=active | 状态（active/repair/scrap/lost/damaged） |

**请求体示例**:

```json
{
  "asset_code": "A001",
  "harddisk_number": 1,
  "harddisk_sn_code": "SN-2026-ABCDE-12345",
  "harddisk_no": 1,
  "harddisk_type": "SSD",
  "harddisk_status": "active",
  "harddisk_sn_description": "系统盘"
}
```

---

#### GET /api/assets/harddisk-sn/{asset_code}/ -- 按资产查硬盘序列号

**权限**: IsAuthenticated

**路径参数**:

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `asset_code` | string | 是 | 资产编码 |

---

#### POST /api/assets/harddisk-sn/search_by_serial_number/ -- 按序列号搜索

**权限**: IsAuthenticated

**请求体**:

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `harddisk_sn_code` | string | 是 | 硬盘序列号 |

> **v1.1.0 变更**: 请求体字段名从 `harddisk_sn` 改为 `harddisk_sn_code`。

**请求体示例**:

```json
{
  "harddisk_sn_code": "SN-2026-ABCDE-12345"
}
```

---

#### POST /api/assets/harddisk-sn/batch/ -- 批量保存硬盘序列号记录

**权限**: IsAuthenticated

**请求体**:

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `asset_code` | string | 是 | 资产编码 |
| `disks` | array | 是 | 硬盘序列号列表 |

**disks 子项**:

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `id` | int | 否 | 硬盘序列号记录 ID（有值时为更新，无值时为新建） |
| `harddisk_no` | int | 否 | 硬盘编号 |
| `harddisk_sn_code` | string | 否 | 序列号 |
| `harddisk_type` | string | 否 | 类型（HDD/SSD/NVMe/Other） |
| `harddisk_status` | string | 否 | 状态（active/repair/scrap/lost/damaged） |

**请求体示例**:

```json
{
  "asset_code": "ASSET-ZDDN-000004",
  "disks": [
    {"harddisk_no": 1, "harddisk_sn_code": "SN001", "harddisk_type": "SSD", "harddisk_status": "active"},
    {"id": 5, "harddisk_no": 2, "harddisk_sn_code": "SN002"}
  ]
}
```

---

#### GET /api/assets/harddisk-sn/by-asset/{asset_code}/ -- 按资产查硬盘序列号

**权限**: IsAuthenticated

**路径参数**:

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `asset_code` | string | 是 | 资产编码 |

---

### 5.13 操作记录

#### GET /api/assets/operation-logs/ -- 操作记录列表

**权限**: IsAuthenticated

**查询参数**:

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `page` | int | 否 | 页码 |
| `page_size` | int | 否 | 每页数量 |
| `asset_code` | string | 否 | 资产编码（精确匹配） |
| `operation_type` | string | 否 | 操作类型（create/update/delete/out/recycle/damaged/waste/approve/transfer） |
| `operator_jobcode` | string | 否 | 操作人工号 |
| `start_date` | date | 否 | 开始日期（YYYY-MM-DD） |
| `end_date` | date | 否 | 结束日期（YYYY-MM-DD） |
| `days` | int | 否 | 最近 N 天（与日期范围互斥） |

**说明**: 只读接口，不允许修改或删除操作记录。

---

#### GET /api/assets/operation-logs/{pk}/ -- 操作记录详情

**权限**: IsAuthenticated

**路径参数**:

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `pk` | int | 是 | 记录 ID |

---

#### GET /api/assets/operation-logs/recent/ -- 最近操作记录

**权限**: IsAuthenticated

**查询参数**:

| 参数 | 类型 | 必填 | 默认值 | 说明 |
|------|------|------|--------|------|
| `days` | int | 否 | 7 | 最近 N 天（范围 1-365） |

---

#### GET /api/assets/operation-logs/user/{operator_jobcode}/ -- 用户操作记录

**权限**: IsAuthenticated

**路径参数**:

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `operator_jobcode` | string | 是 | 操作人工号 |

---

#### GET /api/assets/operation-logs/by-logging-id/{logging_id}/ -- 通过LoggingId查询操作记录

**权限**: IsAuthenticated

**路径参数**:

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `logging_id` | string | 是 | LoggingId |

---

#### GET /api/assets/assets/{asset_code}/history/ -- 资产操作历史

**权限**: IsAuthenticated

**路径参数**:

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `asset_code` | string | 是 | 资产编码 |

---

#### GET /api/assets/assets/{asset_code}/timeline/ -- 资产状态变更时间线

**权限**: IsAuthenticated

**路径参数**:

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `asset_code` | string | 是 | 资产编码 |

---

### 5.14 未登记资产管理

#### GET /api/unregisteredassets/unregistered-assets/ -- 未登记资产列表

**权限**: IsAuthenticated

**查询参数**:

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `page` | int | 否 | 页码 |
| `page_size` | int | 否 | 每页数量 |
| `scene_type` | string | 否 | 场景类型（s1_no_record/s2_no_outasset/s3_status_mismatch） |
| `approval_status` | string | 否 | 审批状态（pending/approved/rejected） |
| `search` | string | 否 | 搜索关键词 |

---

#### POST /api/unregisteredassets/unregistered-assets/ -- 创建未登记资产

**权限**: IsAuthenticated

**请求体**:

| 字段 | 类型 | 必填 | 约束 | 说明 |
|------|------|------|------|------|
| `asset_code` | string | 否 | max_length=30 | 资产编码 |
| `asset_name` | string | 是 | max_length=100 | 资产名称 |
| `asset_type_code` | string | 否 | | 资产类型编码 |
| `asset_brand` | string | 否 | max_length=100 | 品牌 |
| `asset_specification` | string | 否 | max_length=100 | 规格 |
| `scene_type` | string | 是 | max_length=30 | 场景类型（s1_no_record/s2_no_outasset/s3_status_mismatch） |
| `handling_method` | string | 否 | max_length=30 | 处理方式（create_and_recycle/create_and_damaged/supplement_and_recycle/correct_and_recycle/reject） |
| `applicant_jobcode` | string | 否 | | 申请人工号 |
| `approver_jobcode` | string | 否 | | 审批人工号 |
| `approval_status` | string | 否 | default=pending | 审批状态（pending/approved/rejected） |
| `description` | text | 否 | | 描述 |

**请求体示例**:

```json
{
  "asset_name": "未登记笔记本电脑",
  "asset_type_code": "AT001",
  "asset_brand": "Dell",
  "asset_specification": "i5/8G/256G",
  "scene_type": "s1_no_record",
  "handling_method": "create_and_recycle",
  "applicant_jobcode": "E001",
  "description": "盘点时发现的未登记资产"
}
```

---

#### GET /api/unregisteredassets/unregistered-assets/{unregistered_code}/ -- 未登记资产详情

**权限**: IsAuthenticated

**路径参数**:

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `unregistered_code` | string | 是 | 未登记资产编码 |

---

#### PUT/PATCH /api/unregisteredassets/unregistered-assets/{unregistered_code}/ -- 更新未登记资产

**权限**: IsAuthenticated

**路径参数**: 同上

**请求体**: 同创建（PUT 需全部字段，PATCH 仅需修改字段）

---

#### DELETE /api/unregisteredassets/unregistered-assets/{unregistered_code}/ -- 删除未登记资产

**权限**: IsAuthenticated

**路径参数**: 同上

---

#### POST /api/unregisteredassets/unregistered-assets/{unregistered_code}/approve/ -- 审批处理

**权限**: IsAuthenticated

**路径参数**:

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `unregistered_code` | string | 是 | 未登记资产编码 |

**请求体**:

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `approval_status` | string | 是 | 审批结果（approved/rejected） |
| `approver_jobcode` | string | 否 | 审批人工号（默认当前用户） |
| `approval_comment` | string | 否 | 审批意见 |

**请求体示例**:

```json
{
  "approval_status": "approved",
  "approver_jobcode": "E003",
  "approval_comment": "同意创建并回收"
}
```

---

### 5.15 仪表盘

#### GET /api/dashboard/overview/ -- 仪表盘总览

**权限**: IsAuthenticated

**查询参数**: 无

**返回字段**:

| 字段 | 说明 |
|------|------|
| `total_assets` | 资产总数 |
| `active_assets` | 在用资产数 |
| `in_stock_assets` | 在库资产数 |
| `monthly_distributed` | 本月出库数 |
| `monthly_recycled` | 本月回收数 |
| `pending_waste` | 待报废数 |
| `wasted_assets` | 已报废数 |
| `total_recycled` | 累计回收数 |
| `total_distributed` | 累计出库数 |
| `timestamp` | 数据时间戳 |

---

#### GET /api/dashboard/recent_out_assets/ -- 最近出库记录

**权限**: IsAuthenticated

**查询参数**:

| 参数 | 类型 | 必填 | 默认值 | 说明 |
|------|------|------|--------|------|
| `limit` | int | 否 | 10 | 返回数量（最大 100） |

---

#### GET /api/dashboard/recent_recycle_assets/ -- 最近回收记录

**权限**: IsAuthenticated

**查询参数**:

| 参数 | 类型 | 必填 | 默认值 | 说明 |
|------|------|------|--------|------|
| `limit` | int | 否 | 10 | 返回数量（最大 100） |

---

## 六、响应示例

### 6.1 认证管理

#### POST /api/auth/register/ -- 用户注册

**成功响应 (201)**:

```json
{
  "code": 201,
  "msg": "注册成功",
  "data": {
    "user": {
      "auth_id": 1,
      "auth_username": "zhangsan",
      "email": "zhangsan@example.com",
      "auth_is_active": true,
      "auth_is_staff": false,
      "auth_phone": "13800138000",
      "auth_date_create": "2026-05-23T10:00:00+08:00",
      "auth_date_update": "2026-05-23T10:00:00+08:00"
    },
    "refresh": "eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9...",
    "access": "eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9..."
  }
}
```

**失败响应 (400)**:

```json
{
  "code": 400,
  "msg": "注册失败",
  "data": {
    "errors": {
      "password": ["两次输入的密码不一致"]
    }
  }
}
```

---

#### POST /api/auth/login/ -- 用户登录

**成功响应 (200)**:

```json
{
  "code": 200,
  "msg": "登录成功",
  "data": {
    "user": {
      "auth_id": 1,
      "auth_username": "admin",
      "email": "admin@example.com",
      "auth_is_active": true,
      "auth_is_staff": true,
      "auth_phone": "13800138000",
      "auth_date_create": "2026-01-01T08:00:00+08:00",
      "auth_date_update": "2026-05-23T10:00:00+08:00"
    },
    "refresh": "eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9...",
    "access": "eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9..."
  }
}
```

**失败响应 (401)**:

```json
{
  "code": 401,
  "msg": "用户名或密码错误",
  "data": {}
}
```

---

#### POST /api/auth/logout/ -- 退出登录

**成功响应 (200)**:

```json
{
  "code": 200,
  "msg": "退出成功，Token 已作废",
  "data": {}
}
```

---

#### GET /api/auth/profile/ -- 获取当前用户信息

**成功响应 (200)**:

```json
{
  "code": 200,
  "msg": "操作成功",
  "data": {
    "auth_id": 1,
    "auth_username": "admin",
    "email": "admin@example.com",
    "auth_is_active": true,
    "auth_is_staff": true,
    "auth_phone": "13800138000",
    "auth_date_create": "2026-01-01T08:00:00+08:00",
    "auth_date_update": "2026-05-23T10:00:00+08:00"
  }
}
```

---

#### PUT /api/auth/profile/ -- 更新当前用户信息

**成功响应 (200)**:

```json
{
  "code": 200,
  "msg": "更新成功",
  "data": {
    "auth_id": 1,
    "auth_username": "admin",
    "email": "newemail@example.com",
    "auth_is_active": true,
    "auth_is_staff": true,
    "auth_phone": "13900139000",
    "auth_date_create": "2026-01-01T08:00:00+08:00",
    "auth_date_update": "2026-05-23T10:30:00+08:00"
  }
}
```

---

### 6.2 部门管理

#### POST /api/users/departments/ -- 创建部门

**成功响应 (201)**:

```json
{
  "code": 201,
  "msg": "创建成功",
  "data": {
    "department_code": "D001",
    "department_name": "技术部",
    "department_information": "张三",
    "parent_code": null,
    "level": 0,
    "sort_order": 1
  }
}
```

---

#### GET /api/users/departments/ -- 部门列表（分页）

**成功响应 (200)**:

```json
{
  "code": 200,
  "msg": "查询成功",
  "data": {
    "count": 25,
    "total_pages": 2,
    "page": 1,
    "page_size": 20,
    "next": "http://localhost:8000/api/users/departments/?page=2&page_size=20",
    "previous": null,
    "results": [
      {
        "department_code": "D001",
        "department_name": "技术部",
        "department_information": "张三",
        "parent_code": null,
        "level": 0,
        "sort_order": 1
      }
    ]
  }
}
```

---

#### GET /api/users/departments/tree/ -- 部门树形结构

**成功响应 (200)**:

```json
{
  "code": 200,
  "msg": "操作成功",
  "data": [
    {
      "department_code": "D001",
      "department_name": "总公司",
      "level": 0,
      "children": [
        {
          "department_code": "D002",
          "department_name": "技术部",
          "level": 1,
          "children": [],
          "employee_count": 10
        },
        {
          "department_code": "D003",
          "department_name": "市场部",
          "level": 1,
          "children": [],
          "employee_count": 8
        }
      ],
      "employee_count": 18
    }
  ]
}
```

---

#### PUT /api/users/departments/{department_code}/move/ -- 移动部门

**成功响应 (200)**:

```json
{
  "code": 200,
  "msg": "部门移动成功",
  "data": {
    "department_code": "D002",
    "department_name": "技术部",
    "parent_code": "D001",
    "level": 1
  }
}
```

---

### 6.3 员工管理

#### POST /api/users/employees/ -- 创建员工

**成功响应 (201)**:

```json
{
  "code": 201,
  "msg": "创建成功",
  "data": {
    "employee_jobcode": "E001",
    "employee_name": "张三",
    "employee_status": "active",
    "employee_department": "D001",
    "employee_phone": "13800138000",
    "employee_location": "北京总部",
    "employee_description": "技术部开发工程师",
    "sort_order": 0
  }
}
```

---

#### GET /api/users/employees/statistics/ -- 员工统计

**成功响应 (200)**:

```json
{
  "code": 200,
  "msg": "操作成功",
  "data": {
    "total_employees": 50,
    "active_employees": 45,
    "by_status": {
      "active": {"name": "在职员工", "count": 45},
      "left": {"name": "离职员工", "count": 3},
      "retirement": {"name": "退休员工", "count": 2}
    },
    "by_department": {
      "技术部": 15,
      "市场部": 10,
      "财务部": 8,
      "人事部": 5
    }
  }
}
```

---

### 6.4 资产管理

#### POST /api/assets/assets/ -- 创建资产

**成功响应 (201)**:

```json
{
  "code": 201,
  "msg": "创建成功",
  "data": {
    "asset_recordcode": "Entry20260523103000a1b2c3d4",
    "asset_code": "ASSET-hardware-AT001-20260115-abc123-001",
    "asset_name": "ThinkPad X1 Carbon",
    "asset_purchase_price": "8999.00",
    "asset_purchase_number": 1,
    "asset_unit": "台",
    "asset_brand": "Lenovo",
    "asset_specification": "i7/16G/512G SSD",
    "asset_type_code": {
      "asset_type_code": "AT001",
      "asset_type_secondary": "笔记本电脑",
      "asset_type_primary": "计算机设备",
      "asset_type_category": "hardware"
    },
    "asset_contract_code": {
      "contract_code": "CT001",
      "contract_name": "2026年办公设备采购合同"
    },
    "asset_purchase_date": "2026-01-10",
    "asset_warranty_period": 3,
    "asset_entry_date": "2026-01-15",
    "asset_storage_code": {
      "storage_code": "WH001",
      "storage_name": "北京新货仓库"
    },
    "asset_current_status": "in_store",
    "asset_description": null
  }
}
```

---

#### GET /api/assets/assets/search_available/ -- 获取可用资产

**成功响应 (200)**:

```json
{
  "code": 200,
  "msg": "操作成功",
  "data": {
    "count": 30,
    "results": [
      {
        "asset_code": "ASSET-hardware-AT001-20260115-abc123-001",
        "asset_name": "ThinkPad X1 Carbon",
        "asset_current_status": "in_store",
        "asset_storage_code": {"storage_code": "WH001", "storage_name": "北京新货仓库"}
      }
    ]
  }
}
```

---

### 6.5 出库管理

#### POST /api/assets/out-assets/ -- 创建出库记录

**成功响应 (201)**:

```json
{
  "code": 201,
  "msg": "出库成功",
  "data": {
    "outasset_recordcode": "OUT-20260523-XXXXXXXX",
    "outasset_code": "A001",
    "outasset_number": 1,
    "outasset_applicant_jobcode": "E001",
    "outasset_manager_jobcode": "E002",
    "outasset_current_status": "in_use",
    "outasset_using_location": "北京总部3楼技术部",
    "outasset_date": "2026-05-23",
    "outasset_type": "receive",
    "return_date": "2027-01-15",
    "outasset_previous_status": "in_store"
  }
}
```

---

### 6.6 待报废管理

#### POST /api/assets/damaged-assets/{damaged_asset_code}/approve/ -- 审批通过

**成功响应 (200)**:

```json
{
  "code": 200,
  "msg": "审批通过，已报废记录已创建",
  "data": {
    "damaged_asset": {
      "damaged_asset_code": "A001",
      "approval_status": "approved",
      "damaged_date": "2026-05-20"
    },
    "waste_asset": {
      "waste_asset_code": "A001",
      "waste_asset_date": "2026-05-23",
      "waste_asset_number": 1
    }
  }
}
```

---

### 6.7 未登记资产管理

#### POST /api/unregisteredassets/unregistered-assets/ -- 创建未登记资产

**成功响应 (201)**:

```json
{
  "code": 201,
  "msg": "创建成功",
  "data": {
    "unregistered_code": "UNREG-20260608-000001",
    "asset_name": "未登记笔记本电脑",
    "asset_type_code": "AT001",
    "asset_brand": "Dell",
    "asset_specification": "i5/8G/256G",
    "scene_type": "s1_no_record",
    "handling_method": "create_and_recycle",
    "applicant_jobcode": "E001",
    "approval_status": "pending",
    "description": "盘点时发现的未登记资产",
    "created_at": "2026-06-08T10:00:00+08:00"
  }
}
```

---

#### POST /api/unregisteredassets/unregistered-assets/{unregistered_code}/approve/ -- 审批处理

**成功响应 (200)**:

```json
{
  "code": 200,
  "msg": "审批完成",
  "data": {
    "unregistered_code": "UNREG-20260608-000001",
    "approval_status": "approved",
    "approver_jobcode": "E003",
    "approval_comment": "同意创建并回收"
  }
}
```

---

### 6.8 仪表盘

#### GET /api/dashboard/overview/ -- 仪表盘总览

**成功响应 (200)**:

```json
{
  "code": 200,
  "msg": "操作成功",
  "data": {
    "total_assets": 500,
    "active_assets": 350,
    "in_stock_assets": 100,
    "monthly_distributed": 25,
    "monthly_recycled": 10,
    "pending_waste": 5,
    "wasted_assets": 30,
    "total_recycled": 200,
    "total_distributed": 450,
    "timestamp": "2026-06-08T14:30:00+08:00"
  }
}
```

---

### 6.9 通用错误响应

#### 400 -- 请求参数错误

```json
{
  "code": 400,
  "msg": "参数验证失败",
  "data": {
    "errors": {
      "asset_name": ["该字段是必填项。"],
      "asset_purchase_price": ["请输入有效的数字。"]
    }
  }
}
```

#### 404 -- 资源不存在

```json
{
  "code": 404,
  "msg": "请求的资源不存在",
  "data": {}
}
```

#### 403 -- 权限不足

```json
{
  "code": 403,
  "msg": "您没有权限执行此操作",
  "data": {}
}
```

#### 409 -- 资源冲突

```json
{
  "code": 409,
  "msg": "资源冲突",
  "data": {}
}
```

#### 500 -- 服务器内部错误

```json
{
  "code": 500,
  "msg": "操作失败，请稍后重试",
  "data": {}
}
```

---

## 七、数据模型

### 7.1 AuthUser（认证用户）

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| `auth_id` | int | PK, auto | 用户 ID |
| `auth_username` | string(150) | unique, required | 用户名 |
| `password` | string(128) | required, write_only | 密码（仅写入，不返回） |
| `email` | string | optional | 邮箱 |
| `auth_phone` | string | optional | 手机号 |
| `auth_is_active` | bool | default=True | 是否激活 |
| `auth_is_staff` | bool | default=False | 是否管理员 |
| `auth_date_create` | datetime | auto_now_add | 创建时间（只读） |
| `auth_date_update` | datetime | auto_now | 更新时间（只读） |

---

### 7.2 Department（部门）

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| `department_code` | string(20) | PK, unique | 部门编码 |
| `department_name` | string(100) | unique | 部门名称 |
| `department_information` | string(20) | | 部门信息员 |
| `parent_code` | string(20) | nullable, FK(Department) | 上级部门编码 |
| `level` | int | default=0, max=5 | 层级 |
| `sort_order` | int | default=0 | 排序 |

**关联关系**:
- `parent_code` -> Department.department_code（自关联，多对一）
- Employee -> Department（一对多）

---

### 7.3 Employee（员工）

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| `employee_jobcode` | string(20) | PK, unique | 工号 |
| `employee_name` | string(100) | required | 姓名 |
| `employee_status` | string(10) | default=active | 状态（active/left/retirement） |
| `employee_department` | FK(Department) | nullable | 所属部门 |
| `employee_phone` | string(15) | unique | 手机号 |
| `employee_location` | string(100) | | 位置 |
| `employee_description` | text | nullable | 描述 |
| `sort_order` | int | default=0 | 排序 |

**关联关系**:
- `employee_department` -> Department.department_code（多对一）
- Asset -> Employee（多个外键关联）

---

### 7.4 Storage（仓库）

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| `storage_code` | string(20) | PK, unique | 仓库编码 |
| `storage_name` | string(100) | unique | 仓库名称 |
| `storage_address` | string(200) | | 地址 |
| `storage_type` | string(50) | default=newasset | 类型（newasset/recycle/damaged） |
| `storage_description` | text | nullable | 描述 |

---

### 7.5 AssetType（资产类型）

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| `asset_type_code` | string(20) | PK, unique | 类型编码 |
| `asset_type_secondary` | string(100) | | 二级分类 |
| `asset_type_primary` | string(100) | | 一级分类 |
| `asset_type_category` | string(50) | default=hardware | 大类（hardware/software/other/lowvalue） |
| `asset_type_description` | text | nullable | 描述 |

---

### 7.6 Contract（合同）

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| `contract_code` | string(20) | PK, unique | 合同编码 |
| `contract_name` | string(100) | unique | 合同名称 |
| `contract_type` | string(50) | default=purchase | 类型（purchase/service/information_construction/direct_procurement） |
| `contract_price` | decimal(10,2) | required | 合同金额 |
| `contract_supplier` | string(100) | required | 供应商 |
| `contract_signing_date` | date | required | 签订日期 |
| `contract_warranty_period` | int | default=0 | 保修期（年） |
| `contract_preliminary_acceptance_date` | date | nullable | 初验日期 |
| `contract_final_acceptance_date` | date | nullable | 终验日期 |
| `contract_settlment_status` | string(20) | default=pending | 结算状态（pending/settled） |
| `contract_settlment_price` | decimal(10,2) | nullable | 结算金额 |
| `contract_paid_count_number` | int | default=0 | 已付次数 |
| `contract_paid_price` | decimal(10,2) | nullable | 已付金额 |
| `contract_paid_record` | text | nullable | 付款记录 |

---

### 7.7 Asset（资产）

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| `asset_recordcode` | string(32) | unique, auto | 入库记录编码（Entry+时间+UUID） |
| `asset_code` | string(20) | PK, unique, auto | 资产编码（后端自动生成） |
| `asset_name` | string(100) | required | 资产名称 |
| `asset_purchase_price` | decimal(10,2) | required | 采购价格 |
| `asset_purchase_number` | int | default=1 | 采购数量 |
| `asset_unit` | string(50) | nullable | 单位 |
| `asset_brand` | string(100) | nullable | 品牌 |
| `asset_specification` | string(100) | nullable | 规格 |
| `asset_type_code` | FK(AssetType) | required | 资产类型 |
| `asset_contract_code` | FK(Contract) | nullable | 合同 |
| `asset_purchase_date` | date | required | 购买日期 |
| `asset_warranty_period` | int | nullable | 保修期（年） |
| `asset_entry_date` | date | required | 入库日期 |
| `asset_storage_code` | FK(Storage) | nullable | 仓库 |
| `asset_using_location` | string(100) | nullable | 使用地点 |
| `asset_entry_person_jobcode` | FK(Employee) | nullable | 入库人 |
| `asset_applicant_jobcode` | FK(Employee) | nullable | 申请人 |
| `asset_manager_jobcode` | FK(Employee) | nullable | 管理人 |
| `asset_current_status` | string(20) | default=in_store | 状态 |
| `asset_description` | text | nullable | 描述 |

> **v1.1.0 变更**: `asset_code` 改为后端自动生成，格式为 `ASSET-{category}-{type_code}-{YYYYMMDD}-{random}-{seq}`。

**关联关系**:
- `asset_type_code` -> AssetType.asset_type_code
- `asset_contract_code` -> Contract.contract_code
- `asset_storage_code` -> Storage.storage_code
- `asset_entry_person_jobcode` -> Employee.employee_jobcode
- `asset_applicant_jobcode` -> Employee.employee_jobcode
- `asset_manager_jobcode` -> Employee.employee_jobcode

---

### 7.8 OutAsset（出库记录）

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| `outasset_recordcode` | string(36) | unique, auto | 出库记录编码（OUT-日期-UUID） |
| `outasset_code` | FK(Asset) | required | 资产 |
| `outasset_number` | int | default=1 | 出库数量 |
| `outasset_applicant_jobcode` | FK(Employee) | nullable | 申请人（通过 Asset FK 关联） |
| `outasset_manager_jobcode` | FK(Employee) | required | 管理人 |
| `outasset_current_status` | string(20) | default=in_use | 状态（in_use/recycled_pending/damaged/scrapped） |
| `return_date` | date | nullable | 归还日期 |
| `outasset_using_location` | string(200) | | 使用地点（通过 Asset FK 关联） |
| `outasset_date` | date | default=today | 出库日期 |
| `outasset_type` | string(50) | default=receive | 类型（receive/borrow） |
| `outasset_description` | text | nullable | 描述 |
| `outasset_contract_code` | FK(Contract) | nullable | 合同 |
| `outasset_previous_status` | string(20) | nullable | 出库前资产状态 |

> **v1.1.0 变更**: 新增 `return_date`（归还日期）和 `outasset_previous_status`（出库前资产状态）字段。

---

### 7.9 RecycleAsset（回收记录）

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| `outasset_recordcode` | OneToOne(OutAsset) | PK | 出库记录编码 |
| `recycle_asset_code` | FK(Asset) | required | 资产 |
| `recycle_asset_number` | int | default=1 | 回收数量 |
| `recycle_asset_storage_code` | FK(Storage) | required | 回收仓库 |
| `recycle_asset_using_person_jobcode` | FK(Employee) | nullable | 使用人 |
| `recycle_asset_recycle_person_jobcode` | FK(Employee) | required | 回收人 |
| `recycle_asset_date` | date | required | 回收日期 |
| `recycle_asset_description` | text | nullable | 描述 |
| `operator_jobcode` | FK(Employee) | nullable | 操作人工号 |
| `recycle_record_code` | string(36) | unique, auto | 回收记录编码（RECYCLE-YYYYMMDD-XXXXXXXX） |

> **v1.1.0 变更**: 新增 `operator_jobcode`（操作人工号）和 `recycle_record_code`（回收记录编码）字段。

---

### 7.10 DamagedAsset（待报废记录）

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| `damaged_asset_code` | OneToOne(Asset) | unique | 资产编码 |
| `damaged_asset_contract_code` | FK(Contract) | nullable | 合同（通过 Asset FK 关联） |
| `damaged_asset_number` | int | default=1 | 数量 |
| `damaged_asset_storage_code` | FK(Storage) | | 仓库（通过 Asset FK 关联） |
| `damaged_date` | date | default=today | 申请日期 |
| `approval_status` | string(20) | default=pending | 审批状态（pending/approved/rejected） |
| `approver` | FK(Employee) | nullable | 审批人 |
| `damaged_asset_description` | text | nullable | 描述 |

---

### 7.11 WasteAsset（已报废记录）

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| `waste_asset_code` | OneToOne(Asset) | unique | 资产编码 |
| `source_damaged_asset` | OneToOne(DamagedAsset) | nullable | 来源待报废记录 |
| `waste_asset_contract_code` | FK(Contract) | | 合同 |
| `waste_asset_number` | int | default=1 | 数量 |
| `waste_asset_date` | date | required | 报废日期 |
| `waste_asset_description` | text | nullable | 描述 |

---

### 7.12 HardDiskSN（硬盘序列号）

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| `id` | int | PK, auto | 主键 |
| `asset_code` | FK(Asset) | required | 资产 |
| `harddisk_number` | int | required | 硬盘数量 |
| `harddisk_no` | int | default=1 | 硬盘编号 |
| `harddisk_sn_code` | string(100) | unique | 序列号 |
| `harddisk_type` | string(100) | default=HDD | 类型（HDD/SSD/NVMe/Other） |
| `harddisk_sn_description` | text | nullable | 描述 |
| `harddisk_status` | string(10) | default=active | 状态（active/repair/scrap/lost/damaged） |

---

### 7.13 UnregisteredAsset（未登记资产）

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| `unregistered_code` | string(30) | PK, unique, auto | 未登记资产编码 |
| `asset_code` | string(30) | nullable | 资产编码 |
| `asset_name` | string(100) | required | 资产名称 |
| `asset_type_code` | FK(AssetType) | nullable | 资产类型 |
| `asset_brand` | string(100) | nullable | 品牌 |
| `asset_specification` | string(100) | nullable | 规格 |
| `scene_type` | string(30) | required | 场景类型（s1_no_record/s2_no_outasset/s3_status_mismatch） |
| `handling_method` | string(30) | nullable | 处理方式（create_and_recycle/create_and_damaged/supplement_and_recycle/correct_and_recycle/reject） |
| `applicant_jobcode` | FK(Employee) | nullable | 申请人工号 |
| `approver_jobcode` | FK(Employee) | nullable | 审批人工号 |
| `approval_status` | string(20) | default=pending | 审批状态（pending/approved/rejected） |
| `description` | text | nullable | 描述 |
| `created_at` | datetime | auto_now_add | 创建时间（只读） |
| `updated_at` | datetime | auto_now | 更新时间（只读） |

**关联关系**:
- `asset_type_code` -> AssetType.asset_type_code
- `applicant_jobcode` -> Employee.employee_jobcode
- `approver_jobcode` -> Employee.employee_jobcode

---

### 7.14 AssetOperationLog（操作记录，只读）

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| `id` | int | PK, auto | 记录 ID |
| `asset_code` | string(20) | indexed | 资产编码 |
| `operation_type` | string(20) | indexed | 操作类型 |
| `operation_time` | datetime | auto, indexed | 操作时间 |
| `operator_jobcode` | string(20) | nullable | 操作人工号 |
| `operator_name` | string(100) | nullable | 操作人姓名 |
| `before_data` | JSON | nullable | 变更前数据 |
| `after_data` | JSON | nullable | 变更后数据 |
| `description` | text | | 操作描述 |
| `related_record_code` | string(50) | nullable | 关联记录编码 |
| `related_record_type` | string(20) | nullable | 关联记录类型 |
| `ip_address` | GenericIPAddress | nullable | IP 地址 |

---

### 7.15 枚举值汇总

#### 资产状态 (asset_current_status)

| 值 | 中文 | 说明 |
|----|------|------|
| `in_store` | 在库 | 新增加、已拒绝报废 |
| `recycled_pending` | 已回收待发放 | 已回收，等待重新发放 |
| `in_use` | 在用 | 已出库/已发放 |
| `damaged` | 待报废 | 待报废审批中 |
| `scrapped` | 已报废 | 已完成报废 |

#### 出库状态 (outasset_current_status)

| 值 | 中文 | 说明 |
|----|------|------|
| `in_use` | 在用 | 正常使用中 |
| `recycled_pending` | 已回收待处理 | 已提交回收 |
| `damaged` | 待报废 | 已提交报废申请 |
| `scrapped` | 已报废 | 已完成报废 |

> **v1.1.0 新增**: `recycled_pending`、`damaged`、`scrapped`

#### 仓库类型 (storage_type)

| 值 | 中文 |
|----|------|
| `newasset` | 新货仓库 |
| `recycle` | 回收仓库 |
| `damaged` | 待报废仓库 |

#### 合同类型 (contract_type)

| 值 | 中文 |
|----|------|
| `purchase` | 采购合同 |
| `service` | 服务合同 |
| `information_construction` | 信息化建设合同 |
| `direct_procurement` | 直接采购合同 |

#### 合同结算状态 (contract_settlment_status)

| 值 | 中文 |
|----|------|
| `pending` | 待结算 |
| `settled` | 已结算 |

#### 出库类型 (outasset_type)

| 值 | 中文 |
|----|------|
| `receive` | 领用 |
| `borrow` | 借用 |

#### 员工状态 (employee_status)

| 值 | 中文 |
|----|------|
| `active` | 在职员工 |
| `left` | 离职员工 |
| `retirement` | 退休员工 |

#### 审批状态 (approval_status)

| 值 | 中文 |
|----|------|
| `pending` | 待审批 |
| `approved` | 已批准 |
| `rejected` | 已拒绝 |

#### 硬盘状态 (harddisk_status)

| 值 | 中文 |
|----|------|
| `active` | 正常 |
| `repair` | 维修 |
| `scrap` | 报废 |
| `lost` | 丢失 |
| `damaged` | 损坏 |

#### 硬盘类型 (harddisk_type)

| 值 | 中文 |
|----|------|
| `HDD` | HDD |
| `SSD` | SSD |
| `NVMe` | NVMe |
| `Other` | 其他 |

#### 操作类型 (operation_type)

| 值 | 中文 |
|----|------|
| `create` | 创建 |
| `update` | 更新 |
| `delete` | 删除 |
| `out` | 出库 |
| `recycle` | 回收 |
| `damaged` | 待报废 |
| `waste` | 已报废 |
| `approve` | 审批 |
| `transfer` | 转移 |

#### 资产分类 (asset_type_category)

| 值 | 中文 |
|----|------|
| `hardware` | 硬件 |
| `software` | 软件 |
| `other` | 其他 |
| `lowvalue` | 低值易耗 |

> **v1.1.0 新增**: `lowvalue`（低值易耗）

#### 未登记资产场景类型 (scene_type)

| 值 | 中文 | 说明 |
|----|------|------|
| `s1_no_record` | 无入库记录 | 资产实物存在但系统中无入库记录 |
| `s2_no_outasset` | 无出库记录 | 资产在用但系统中无出库记录 |
| `s3_status_mismatch` | 状态不匹配 | 资产实际状态与系统记录不一致 |

#### 未登记资产处理方式 (handling_method)

| 值 | 中文 | 说明 |
|----|------|------|
| `create_and_recycle` | 创建并回收 | 补建入库记录并创建回收记录 |
| `create_and_damaged` | 创建并报废 | 补建入库记录并创建待报废申请 |
| `supplement_and_recycle` | 补录并回收 | 补充出库记录并创建回收记录 |
| `correct_and_recycle` | 纠正并回收 | 纠正状态并创建回收记录 |
| `reject` | 驳回 | 驳回未登记资产处理 |

---

## 八、特殊功能说明

### 8.1 分页机制

系统采用自定义分页器 `CustomPageNumberPagination`，具有以下特性：

- **默认每页**: 20 条
- **最大每页**: 100 条
- **参数名**: `page`（页码）、`page_size`（每页数量）
- **智能分页**: 无分页参数时返回全部数据（不分页），有分页参数时启用分页

**使用示例**:

```
# 不分页，返回全部数据
GET /api/assets/assets/

# 分页，返回第 1 页，每页 20 条
GET /api/assets/assets/?page=1

# 分页，返回第 2 页，每页 50 条
GET /api/assets/assets/?page=2&page_size=50
```

### 8.2 过滤搜索

系统使用 `django-filter` + `django-rest-framework` 内置过滤器实现多维度的过滤和搜索。

**精确过滤** (`filterset_fields`):

通过 `DjangoFilterBackend` 实现精确匹配过滤，参数名与字段名一致。

```
GET /api/assets/assets/?asset_current_status=in_store&asset_type_code=AT001
```

**模糊搜索** (`search_fields`):

通过 `SearchFilter` 实现，使用 `search` 参数，在指定字段中进行 `icontains` 查询。

```
GET /api/users/employees/?search=张三
```

**关键词搜索** (`keyword` 参数):

部分视图集支持自定义的 `keyword` 参数，在多个字段中进行 OR 查询。

```
GET /api/assets/assets/?keyword=ThinkPad
GET /api/assets/storages/?keyword=北京
```

### 8.3 排序

通过 `OrderingFilter` 实现，使用 `ordering` 参数。

```
# 按入库日期降序（默认）
GET /api/assets/assets/?ordering=-asset_entry_date

# 按资产编码升序
GET /api/assets/assets/?ordering=asset_code

# 按价格降序
GET /api/assets/assets/?ordering=-asset_purchase_price
```

### 8.4 批量操作

#### 部门批量排序

```
PUT /api/users/departments/sort/
```

一次性更新多个部门的排序值，适用于前端拖拽排序场景。

#### 员工批量排序

```
PUT /api/users/employees/sort/
```

一次性更新多个员工的排序值。

#### 资产批量创建

```
POST /api/assets/assets/
```

传入数组格式的请求体，可一次性创建多个资产记录，后端自动为每个资产生成唯一编码。

#### 硬盘序列号批量保存

```
POST /api/assets/harddisk-sn/batch/
```

一次性保存或更新某个资产下的多个硬盘序列号记录。

### 8.5 软删除

系统中大部分资源采用软删除机制（通过 `is_deleted` 字段标记），删除操作不会物理删除数据库记录。

**支持软删除的资源**:
- 部门 (Department)
- 员工 (Employee)
- 仓库 (Storage)
- 资产类型 (AssetType)
- 合同 (Contract)
- 资产 (Asset)
- 出库记录 (OutAsset)
- 回收记录 (RecycleAsset)
- 待报废记录 (DamagedAsset)

**注意**: 操作记录 (AssetOperationLog) 为只读，不支持删除。

### 8.6 资产状态流转

资产在其生命周期中经历以下状态流转：

```
                    +------------------+
                    |    in_store      |
                    |    （在库）       |
                    +--------+---------+
                             |
                             | 出库
                             v
                    +------------------+
                    |    in_use        |
                    |    （在用）       |
                    +--------+---------+
                             |
              +--------------+--------------+
              |                             |
              | 回收                        | 提交报废申请
              v                             v
    +------------------+          +------------------+
    | recycled_pending |          |    damaged       |
    | （已回收待发放）  |          |    （待报废）     |
    +--------+---------+          +--------+---------+
             |                             |
             | 重新发放                    | 审批通过
             v                             v
    +------------------+          +------------------+
    |    in_use        |          |    scrapped      |
    |    （在用）       |          |    （已报废）     |
    +------------------+          +------------------+
                                         ^
    +------------------+                 |
    | recycled_pending |  审批拒绝        |
    | （已回收待发放）  | <---------------+
    +------------------+
```

**状态流转规则**:

| 当前状态 | 目标状态 | 触发操作 |
|----------|----------|----------|
| in_store | in_use | 出库 |
| in_use | recycled_pending | 回收 |
| recycled_pending | in_use | 重新发放 |
| in_use | damaged | 提交报废申请 |
| damaged | scrapped | 审批通过 |
| damaged | recycled_pending | 审批拒绝 |

### 8.7 自动编码生成

系统在创建特定记录时自动生成编码：

| 编码类型 | 格式 | 示例 |
|----------|------|------|
| 资产入库记录编码 | `Entry` + `YYYYMMDDHHmmss` + 8位UUID | `Entry20260523103000a1b2c3d4` |
| 资产编码 | `ASSET-` + `{category}` + `-` + `{type_code}` + `-` + `YYYYMMDD` + `-` + `{random}` + `-` + `{seq}` | `ASSET-hardware-AT001-20260115-abc123-001` |
| 出库记录编码 | `OUT-` + `YYYYMMDD` + `-` + 8位UUID | `OUT-20260523-XXXXXXXX` |
| 回收记录编码 | `RECYCLE-` + `YYYYMMDD` + `-` + 8位UUID | `RECYCLE-20260523-XXXXXXXX` |

> **v1.1.0 变更**: 新增资产编码自动生成格式 `ASSET-{category}-{type_code}-{YYYYMMDD}-{random}-{seq}`；新增回收记录编码格式 `RECYCLE-YYYYMMDD-XXXXXXXX`。

### 8.8 操作日志

系统自动记录资产的关键操作日志，包括：

- **记录时机**: 创建、更新、删除、出库、回收、待报废、已报废、审批
- **记录内容**: 操作前数据、操作后数据、操作人、操作时间、IP 地址
- **查询维度**: 按资产、按操作人、按时间范围、按操作类型、按 LoggingId
- **只读**: 操作日志不支持修改和删除

---

## 九、错误码完整清单

### 9.1 HTTP 状态码

| 状态码 | 含义 | 使用场景 |
|--------|------|----------|
| 200 | OK | 请求成功 |
| 201 | Created | 资源创建成功 |
| 400 | Bad Request | 请求参数错误、数据验证失败、业务逻辑错误 |
| 401 | Unauthorized | 未认证、Token 无效或过期、用户名或密码错误 |
| 403 | Forbidden | 权限不足 |
| 404 | Not Found | 资源不存在 |
| 409 | Conflict | 资源冲突 |
| 500 | Internal Server Error | 服务器内部错误 |

### 9.2 自定义业务错误码

| 错误码 | 异常类 | HTTP 状态码 | 说明 | 示例场景 |
|--------|--------|-------------|------|----------|
| E001 | AppValidationError | 400 | 数据验证失败 | 必填字段缺失、格式错误、密码不符合要求 |
| E002 | NotFoundError | 404 | 资源不存在 | 查询的资产编码不存在 |
| E003 | PermissionDeniedError | 403 | 权限不足 | 普通用户尝试删除部门 |
| E004 | BusinessLogicError | 400 | 业务逻辑错误 | 库存不足、状态不允许流转 |
| E005 | ResourceConflictError | 409 | 资源冲突 | 重复创建、并发修改 |
| E006 | InternalError | 500 | 服务器内部错误 | 未知异常 |

### 9.3 常见错误场景

| 场景 | 错误码 | 说明 |
|------|--------|------|
| 注册时密码不一致 | E001 (400) | password 与 password2 不匹配 |
| 密码不符合强度要求 | E001 (400) | Django validate_password 验证失败 |
| 登录用户名或密码错误 | 401 | 认证失败 |
| Token 过期 | 401 | access_token 已过期，需使用 refresh_token 刷新 |
| 未携带 Token | 401 | 请求头缺少 Authorization |
| 普通用户执行管理操作 | 403 | 需要管理员权限 |
| 查询不存在的资源 | E002 (404) | 资产/部门/员工编码不存在 |
| 删除有关联资产的合同 | E004 (400) | 业务逻辑阻止删除 |
| 部门移动导致循环引用 | E004 (400) | 不能移动到自己的子部门下 |
| 部门移动超出层级限制 | E004 (400) | 移动后超过 6 层 |
| 重复的编码 | E005 (409) | 唯一字段冲突 |

---

## 十、实用补充内容

### 10.1 cURL 调用示例

#### 用户注册

```bash
curl -X POST http://localhost:8000/api/auth/register/ \
  -H "Content-Type: application/json" \
  -d '{
    "auth_username": "zhangsan",
    "password": "StrongP@ssw0rd!",
    "password2": "StrongP@ssw0rd!",
    "email": "zhangsan@example.com",
    "auth_phone": "13800138000"
  }'
```

#### 用户登录

```bash
curl -X POST http://localhost:8000/api/auth/login/ \
  -H "Content-Type: application/json" \
  -d '{
    "auth_username": "zhangsan",
    "password": "StrongP@ssw0rd!"
  }'
```

#### 获取当前用户信息

```bash
curl -X GET http://localhost:8000/api/auth/profile/ \
  -H "Authorization: Bearer <access_token>"
```

#### 创建资产（v1.1.0: asset_code 由后端自动生成）

```bash
curl -X POST http://localhost:8000/api/assets/assets/ \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer <access_token>" \
  -d '{
    "asset_name": "ThinkPad X1 Carbon",
    "asset_purchase_price": "8999.00",
    "asset_purchase_date": "2026-01-10",
    "asset_entry_date": "2026-01-15",
    "asset_type_code": "AT001",
    "asset_contract_code": "CT001",
    "asset_storage_code": "WH001",
    "asset_entry_person_jobcode": "E001",
    "asset_brand": "Lenovo",
    "asset_specification": "i7/16G/512G SSD",
    "asset_unit": "台",
    "asset_warranty_period": 3
  }'
```

#### 创建出库记录

```bash
curl -X POST http://localhost:8000/api/assets/out-assets/ \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer <access_token>" \
  -d '{
    "outasset_code": "A001",
    "outasset_manager_jobcode": "E002",
    "outasset_type": "receive",
    "return_date": "2027-01-15"
  }'
```

#### 刷新 Token

```bash
curl -X POST http://localhost:8000/api/auth/token/refresh/ \
  -H "Content-Type: application/json" \
  -d '{
    "refresh": "<refresh_token>"
  }'
```

#### 退出登录

```bash
curl -X POST http://localhost:8000/api/auth/logout/ \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer <access_token>" \
  -d '{
    "refresh": "<refresh_token>"
  }'
```

### 10.2 Python 调用示例

```python
import requests

BASE_URL = "http://localhost:8000/api"

# ===== 1. 用户注册 =====
response = requests.post(f"{BASE_URL}/auth/register/", json={
    "auth_username": "zhangsan",
    "password": "StrongP@ssw0rd!",
    "password2": "StrongP@ssw0rd!",
    "email": "zhangsan@example.com"
})
print("注册结果:", response.json())

# ===== 2. 用户登录 =====
response = requests.post(f"{BASE_URL}/auth/login/", json={
    "auth_username": "zhangsan",
    "password": "StrongP@ssw0rd!"
})
data = response.json()["data"]
access_token = data["access"]
refresh_token = data["refresh"]
headers = {"Authorization": f"Bearer {access_token}"}

# ===== 3. 获取当前用户信息 =====
response = requests.get(f"{BASE_URL}/auth/profile/", headers=headers)
print("用户信息:", response.json())

# ===== 4. 获取资产列表（分页） =====
response = requests.get(
    f"{BASE_URL}/assets/assets/",
    headers=headers,
    params={"page": 1, "page_size": 20, "asset_current_status": "in_store"}
)
print("资产列表:", response.json())

# ===== 5. 创建资产（v1.1.0: asset_code 由后端自动生成） =====
response = requests.post(
    f"{BASE_URL}/assets/assets/",
    headers=headers,
    json={
        "asset_name": "ThinkPad X1 Carbon",
        "asset_purchase_price": "8999.00",
        "asset_purchase_date": "2026-01-10",
        "asset_entry_date": "2026-01-15",
        "asset_type_code": "AT001",
        "asset_storage_code": "WH001",
        "asset_entry_person_jobcode": "E001",
        "asset_brand": "Lenovo",
        "asset_specification": "i7/16G/512G SSD",
        "asset_unit": "台",
        "asset_warranty_period": 3
    }
)
print("创建资产:", response.json())

# ===== 6. 获取仪表盘概览 =====
response = requests.get(f"{BASE_URL}/dashboard/overview/", headers=headers)
print("仪表盘:", response.json())

# ===== 7. 刷新 Token =====
response = requests.post(f"{BASE_URL}/auth/token/refresh/", json={
    "refresh": refresh_token
})
new_access_token = response.json()["access"]
print("新 Token:", new_access_token)

# ===== 8. 退出登录 =====
response = requests.post(
    f"{BASE_URL}/auth/logout/",
    headers={"Authorization": f"Bearer {new_access_token}"},
    json={"refresh": refresh_token}
)
print("退出结果:", response.json())
```

### 10.3 JavaScript (fetch) 调用示例

```javascript
const BASE_URL = 'http://localhost:8000/api';

// ===== 1. 用户登录 =====
async function login(username, password) {
  const response = await fetch(`${BASE_URL}/auth/login/`, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ auth_username: username, password })
  });
  const result = await response.json();
  if (result.code === 200) {
    localStorage.setItem('access_token', result.data.access);
    localStorage.setItem('refresh_token', result.data.refresh);
  }
  return result;
}

// ===== 2. 通用请求封装 =====
async function apiRequest(method, url, data = null) {
  const token = localStorage.getItem('access_token');
  const options = {
    method,
    headers: {
      'Content-Type': 'application/json',
      ...(token ? { Authorization: `Bearer ${token}` } : {})
    }
  };
  if (data) options.body = JSON.stringify(data);
  const response = await fetch(`${BASE_URL}${url}`, options);
  return await response.json();
}

// ===== 3. 获取资产列表 =====
async function getAssets(page = 1, pageSize = 20) {
  return apiRequest('GET', `/assets/assets/?page=${page}&page_size=${pageSize}`);
}

// ===== 4. 创建出库记录 =====
async function createOutAsset(assetCode, managerJobcode) {
  return apiRequest('POST', '/assets/out-assets/', {
    outasset_code: assetCode,
    outasset_manager_jobcode: managerJobcode,
    outasset_type: 'receive'
  });
}

// ===== 5. 获取仪表盘概览 =====
async function getDashboardOverview() {
  return apiRequest('GET', '/dashboard/overview/');
}

// ===== 6. 刷新 Token =====
async function refreshToken() {
  const refresh = localStorage.getItem('refresh_token');
  const result = await fetch(`${BASE_URL}/auth/token/refresh/`, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ refresh })
  }).then(r => r.json());
  if (result.access) {
    localStorage.setItem('access_token', result.access);
  }
  return result;
}

// ===== 7. 退出登录 =====
async function logout() {
  const token = localStorage.getItem('access_token');
  const refresh = localStorage.getItem('refresh_token');
  const result = await apiRequest('POST', '/auth/logout/', { refresh });
  localStorage.removeItem('access_token');
  localStorage.removeItem('refresh_token');
  return result;
}
```

### 10.4 版本说明

| 版本 | 日期 | 说明 |
|------|------|------|
| v1.1.0 | 2026-06-08 | 新增未登记资产管理模块；新增18个端点；删除 search_by_name 端点；修改5个创建端点字段；新增枚举值；更新自动编码格式 |
| v1.0.0 | 2026-05-23 | 初始版本，包含完整的资产全生命周期管理 API |

### 10.5 环境说明

| 环境 | 基础 URL | 说明 |
|------|----------|------|
| 开发环境 | `http://localhost:8000/api/` | 本地开发调试 |
| 生产环境 | 根据部署配置 | 需配置 HTTPS |

### 10.6 注意事项

1. **日期格式**: 所有日期字段使用 `YYYY-MM-DD` 格式（如 `2026-01-15`）
2. **日期时间格式**: datetime 字段使用 ISO 8601 格式（如 `2026-06-08T10:00:00+08:00`）
3. **金额格式**: 金额字段使用字符串形式的 decimal（如 `"8999.00"`），避免浮点精度问题
4. **分页行为**: 不传分页参数时返回全部数据，传 `page` 或 `page_size` 时启用分页
5. **软删除**: 删除操作为软删除，数据不会物理删除
6. **操作日志**: 系统自动记录操作日志，日志为只读，不可修改或删除
7. **资产编码**: `asset_code` 由后端自动生成，创建后不可修改
8. **自动编码**: `asset_recordcode`、`asset_code`、`outasset_recordcode`、`recycle_record_code` 由系统自动生成
9. **权限控制**: 部门和员工的管理操作（创建/修改/删除）需要管理员权限
10. **Token 安全**: `access_token` 有效期较短（默认 2 小时），过期后需使用 `refresh_token` 刷新

---

> 文档结束。如有疑问，请联系后端开发团队。
