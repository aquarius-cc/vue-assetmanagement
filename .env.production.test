VITE_API_BASE_URL=https://api.mycompany.com/api/v1

# 部署路径契约：预发同样根路径部署，与生产保持一致（H-部署base路径固化）
VITE_BASE_URL=/
# VITE_TOKEN_CRYPTO_KEY=（勿提交真实密钥）

# 认证通道：auto=按设备自动检测（PC→cookie、移动端→bearer）
VITE_AUTH_CHANNEL=auto
