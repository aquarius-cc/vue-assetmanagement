
---

### 📄 文档 3：前端子引擎 `/vue-assetmanagement/AGENTS.md` (v9.3.0)

# 前端 AI 执行子引擎配置 (Frontend Engine)
> 版本：v9.3.0 | 最后更新：2026-07-09
> 职责：Vue 3 组件开发、设计令牌执行、UI 交互逻辑、前端测试门禁、组件规模与复杂度管控

## §1 核心执行协议

### 1.1 读取优先
- 执行任务前，**必须**读取 `../Rules_Fiels/frontend-business-rules.md` 并逐条核对 F1-F15 及 FR-1~FR-7。
- 执行任务前，**必须**读取 `../Rules_Fiels/frontend-testing-rules.md` 并逐条核对 T9-T16（含变异测试，变异测试工具统一使用 Vitest 插件）。
- **必须**读取根级 `/AGENTS.md` 的 §6（安全）、§7（可观测性）、§8（AI鲁棒性），并确保生成的代码符合所有相关条款。

### 1.2 技术栈硬约束
- **语法**：强制 `<script setup lang="ts">`，**禁止** Options API。
- **状态**：使用 Pinia Setup Store，**禁止**组件内直接使用 axios。
- **样式**：组件样式必须 `scoped`，覆盖 Element Plus 变量需集中在 `element-variables.scss`。

### 1.3 硬性代码标准与质量门禁
- **设计令牌零容忍**：颜色、间距、圆角、字体**必须**来源于 F1-F5 的定义。**禁止**在任何组件中硬编码 `#fff`、`#000` 或非 4 倍数的间距值。
- **类型检查**：必须通过 `tsc --noEmit`。
- **代码规范**：必须通过 `npm run lint`（ESLint）。
- **复杂度门禁**：代码完成后，必须运行 `eslint . --ext .vue,.ts --rule "complexity: ['error', 10]"`，若超标则触发 `[HALT]`。（若未配置 ESLint 复杂度规则，须在审计票中人工核验函数行数/嵌套深度）。
- **测试门禁**：代码完成后，**必须**运行 `vitest --coverage --threshold 80`，并单独检查 Store 层覆盖率 ≥ 90%。若未通过，触发 `[HALT]` 并补充测试用例。
- **变异测试**：必须运行 `npx vitest --mutate`（Vitest 变异测试插件）并确保通过率 ≥ 80%（参见 T16）。**禁止**引入 Stryker 等外部变异测试工具。
- **复用规则索引**：编码前必须检查是否存在可复用的组件/Composables/Utils，详细复用规范见 `../Rules_Fiels/frontend-business-rules.md` 第四节（FR-1 ~ FR-4）。
- **规模规则索引**：组件行数、Composable 规模、模板嵌套深度等量化红线见 `../Rules_Fiels/frontend-business-rules.md` 第四节（FR-5 ~ FR-7）。

## §2 规则索引
| 规范类型 | 文件路径 |
|:---|:---|
| 业务规范 | `../Rules_Fiels/frontend-business-rules.md` (F1-F15 + 暗色模式 + FR-1~FR-7) |
| 测试规范 | `../Rules_Fiels/frontend-testing-rules.md` (T9-T16) |

## §3 最终审计票（前端专用）

### 必填项

```markdown
[审计票-前端 - 必填项]
- 读取规范：已读 AGENTS & Rules
- 组件语法：Setup Script [√]
- 设计令牌：符合 F1-F5 [√]
- 样式隔离：scoped / 未污染全局 [√]
- CT-1[√] CT-3[√] CT-5[√]
- DR-1[√] DR-5[√/豁免]
- SC-1[√] SC-3[√]
- 跨端契约：未破坏
- 红线触发：无 / [HALT]已确认
- 建议提交：是 / 否
```

### 自检项

```markdown
[审计票-前端 - 自检项]
- 测试：CT-2[√] CT-4[√] CT-6[N/A]（前端无数据库迁移，不适用）
- DRY：DR-2~DR-4[√] DR-6[√]
- 安全：SC-2[√] SC-4~SC-8[√]
- 可观测性：OC-1~OC-3[√] OC-4[~] OC-5[~] OC-6[√] OC-7[~]
- AI鲁棒性：AR-1~AR-5[√]
- 覆盖率：整体 XX% / Store 层 XX%
```
## §4 子域规则演进协议（前端局部自适应）
本协议仅管理前端专属规范。若变更涉及跨端契约，必须上报根级。

### 4.1 自主变更范围
- 修改 F1-F5 设计令牌（色值、间距、圆角、字体）。

- 新增/废弃 Element Plus 组件封装规则。

- 调整 Pinia Store 结构或命名约定。

- 修改 Vite/TypeScript 工程化配置标准。

- 修改 FR-1~FR-7 中的前端量化阈值或复用规则。

- 调整前端测试细则 T9-T16（如新增测试目录规则、变异测试配置）。

### 4.2 变更执行步骤
1. **差异分析**：对比新需求与 frontend-business-rules.md 或 frontend-testing-rules.md。

2. **影响评估**：统计受影响的 .vue / .scss / tests/ 文件数量。

3. **生成补丁**：拟定对目标文件的修改，标记 `[PATCH-FE]`。

4. **[HALT] 审批**：展示补丁内容，等待用户确认。

5. **应用与追溯**：

    - 更新文件头部版本号（如 v1.0 → v1.1）并追加变更日志。

    - 若本次修改涉及 AGENTS.md 自身流程/规则的调整，头部版本号须 +1（如 v9.0.0 → v9.1.0），并在本文件末尾"变更日志"中记录。

### 4.3 冲突上报规则
- 涉及后端 API 字段结构调整（如状态枚举值改动）→ 上报根级。

- 涉及统一响应结构变化 → 上报根级。

- 涉及全局 CSS 变量命名空间变更，可能影响其他端 → 上报根级。

## §5 变更日志
- **v9.3.0 (2026-07-09)**：修复 T8 ID 冲突——前端测试规则编号从 T8-T15 重编号为 T9-T16，消除与后端 T8 的重叠。

- **v9.2.0 (2026-07-09)**：修复 S-6——对齐前端测试规则编号（T13/T14 拆分为独立规则）；变异测试工具明确为 Vitest 插件，禁止引入 Stryker；审计票 CT-6[N/A] 补充不适用说明。

- **v9.1.0 (2026-07-07)**：同步根级v3.1.0，审计票增加CT-6和OC-7，OC-5标记改为[~]。

- v9.0.0 (2026-07-07): 适配根级 v3.0.0，增加安全/可观测性/AI鲁棒性契约引用；审计票扩展；增加变异测试门禁（T15）。

- v8.3.0 (2026-07-07): 新增量化阈值规范（FR-5~FR-7）及对应的复杂度门禁命令（ESLint complexity）。

- v8.2.0 (2026-07-07): 新增测试门禁与 frontend-testing-rules.md 索引；§4 追加自身版本变更追溯规则。

- v8.1.1 (2026-07-07): 初始版本，建立前端令牌约束与局部自适应协议。